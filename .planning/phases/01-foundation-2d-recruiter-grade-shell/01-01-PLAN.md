---
phase: 01-foundation-2d-recruiter-grade-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - eslint.config.js
  - .prettierrc.json
  - .prettierignore
  - .gitignore
  - .editorconfig
  - index.html
  - src/app/main.tsx
  - src/app/App.tsx
  - src/styles/tokens.css
autonomous: true
requirements:
  - FND-01

must_haves:
  truths:
    - "Repository has a working Vite 8 + React 19 dev server (`npm run dev` starts and serves index.html)"
    - "Repository has a passing `tsc --noEmit` typecheck"
    - "Repository has a passing `eslint .` lint pass"
    - "Repository has a passing `prettier --check .` format pass"
    - "Repository has a passing `vite build` production build"
    - "Tailwind v4 `@theme` tokens compile and resolve to the eight UI-SPEC HEX values"
    - "JetBrains Mono (weights 400 + 600) is bundled by Vite from `@fontsource/jetbrains-mono`"
    - "`base: '/Portfolio_Website/'` is set in vite.config.ts so production assets resolve under the GH Pages prefix"
    - "Sourcemaps are disabled in production builds (`build.sourcemap: false`)"
  artifacts:
    - path: "package.json"
      provides: "Dependency manifest + scripts (dev, build, preview, typecheck, lint, lint:fix, format, format:check, test, test:watch, test:coverage, encode-email)"
      contains: '"type": "module"'
    - path: "vite.config.ts"
      provides: "Vite config with base + react + tailwindcss plugins, sourcemap off"
      contains: "base: '/Portfolio_Website/'"
    - path: "tsconfig.json"
      provides: "TypeScript strict mode for src/"
      contains: '"strict": true'
    - path: "tsconfig.node.json"
      provides: "TypeScript config for vite.config.ts type-checking"
      contains: "vite.config.ts"
    - path: "eslint.config.js"
      provides: "ESLint 9 flat config with TS + react-hooks + react-refresh + jsx-a11y + prettier-config"
      contains: "tseslint.config"
    - path: ".prettierrc.json"
      provides: "Prettier 3 formatting config (2 spaces, single quotes, trailing commas all)"
      contains: '"singleQuote": true'
    - path: "src/styles/tokens.css"
      provides: "Tailwind v4 entrypoint + @theme block with UI-SPEC HEX tokens"
      contains: '--color-bg:        #0d1117'
    - path: "src/app/main.tsx"
      provides: "React 19 createRoot bootstrap + font CSS imports + tokens.css import"
      contains: '@fontsource/jetbrains-mono/400.css'
    - path: "src/app/App.tsx"
      provides: "App component placeholder (later finalized in Plan 04)"
      min_lines: 5
    - path: "index.html"
      provides: "Vite entry HTML (minimal in this plan; Plan 06 expands the head with SEO + JSON-LD)"
      contains: '<div id="root"></div>'
    - path: ".gitignore"
      provides: "Ignores node_modules, dist, coverage, .DS_Store, .env*"
      contains: "node_modules"
  key_links:
    - from: "src/app/main.tsx"
      to: "src/styles/tokens.css"
      via: "ESM import"
      pattern: "import.*tokens.css"
    - from: "src/app/main.tsx"
      to: "@fontsource/jetbrains-mono/{400,600}.css"
      via: "ESM import"
      pattern: "@fontsource/jetbrains-mono"
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "plugin import"
      pattern: "tailwindcss\\(\\)"
    - from: "vite.config.ts"
      to: "@vitejs/plugin-react"
      via: "plugin import"
      pattern: "react\\(\\)"
---

<objective>
Scaffold the Vite 8 + React 19 + TypeScript 5.9 + Tailwind v4 project with all build/lint/format gates working from green-state. After this plan, every later plan can author code knowing `npm run dev`, `npm run build`, `npx tsc --noEmit`, `npx eslint .`, and `npx prettier --check .` all pass on a near-empty tree.

Purpose: Phase 1's deploy pipeline (Plan 02), every utility (Plan 03), every UI primitive (Plan 04), every section (Plan 05), and every static asset (Plan 06) run on top of this scaffold. Without it, no other plan in Phase 1 can begin.

Output: A buildable, lintable, formattable React 19 + Vite 8 + Tailwind v4 SPA that renders a single `<div>` (placeholder for Plan 04's App finalization), with all eight UI-SPEC color tokens encoded as Tailwind v4 `@theme` custom properties and JetBrains Mono self-hosted via `@fontsource`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-2d-recruiter-grade-shell/01-CONTEXT.md
@.planning/phases/01-foundation-2d-recruiter-grade-shell/01-RESEARCH.md
@.planning/phases/01-foundation-2d-recruiter-grade-shell/01-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- This plan creates new files; no upstream interfaces to extract.
     Subsequent plans depend on the following from this plan: -->

The Tailwind v4 `@theme` tokens declared in `src/styles/tokens.css` produce the following Tailwind utility classes (consumers in Plans 04, 05, 06 must use these):
- `bg-bg`, `text-bg`            (--color-bg:        #0d1117)
- `bg-fg`, `text-fg`            (--color-fg:        #c9d1d9)
- `bg-accent`, `text-accent`    (--color-accent:    #7ee787)
- `bg-warn`, `text-warn`        (--color-warn:      #e3b341)
- `bg-negative`, `text-negative`(--color-negative:  #ff7b72)
- `bg-muted`, `text-muted`      (--color-muted:     #8b949e)
- `bg-surface-1`                (--color-surface-1: #161b22)
- `outline-focus`, `ring-focus` (--color-focus:     #79c0ff)
- `font-mono`                   (--font-mono: JetBrains Mono fallback chain)

The base path constant for asset URLs (consumers in Plans 03, 04, 05, 06):
- Vite injects `import.meta.env.BASE_URL` from `vite.config.ts`'s `base` value
- In dev:  `/`
- In prod: `/Portfolio_Website/`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold package.json, configs, and Vite/TS/ESLint/Prettier toolchain</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.node.json, eslint.config.js, .prettierrc.json, .prettierignore, .gitignore, .editorconfig</files>

  <read_first>
    - .planning/phases/01-foundation-2d-recruiter-grade-shell/01-RESEARCH.md (sections "Standard Stack", "Pattern 1: Vite Config for GitHub Pages", "Pattern 10: ESLint 9 Flat Config", "Pattern 12: Package.json Scripts")
    - .planning/phases/01-foundation-2d-recruiter-grade-shell/01-CONTEXT.md (D-15, D-16, D-18 for CI gates and deploy expectations)
    - CLAUDE.md (Technology Stack table — pinned versions for React 19.2.x, Vite 8.x, TypeScript 5.9.x, Tailwind 4.2.x, ESLint 9.x)
  </read_first>

  <action>
    Create the full Phase 1 toolchain. Use the EXACT pinned versions from RESEARCH.md "Standard Stack" tables. All versions use tilde `~` ranges (allow patch updates only) — NEVER caret `^`.

    **Step 1.1 — Create `package.json`:**

    ```json
    {
      "name": "portfolio-website",
      "private": true,
      "version": "0.0.0",
      "type": "module",
      "engines": {
        "node": ">=22.12.0"
      },
      "scripts": {
        "dev": "vite",
        "build": "tsc --noEmit && vite build",
        "preview": "vite preview",
        "typecheck": "tsc --noEmit",
        "lint": "eslint .",
        "lint:fix": "eslint . --fix",
        "format": "prettier --write .",
        "format:check": "prettier --check .",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:coverage": "vitest run --coverage",
        "encode-email": "node scripts/encode-email.mjs"
      },
      "dependencies": {
        "react": "~19.2.5",
        "react-dom": "~19.2.5",
        "@fontsource/jetbrains-mono": "~5.2.8"
      },
      "devDependencies": {
        "@types/react": "~19.2.14",
        "@types/react-dom": "~19.2.3",
        "@types/node": "~25.6.0",
        "vite": "~8.0.10",
        "@vitejs/plugin-react": "~6.0.1",
        "typescript": "~5.9.3",
        "tailwindcss": "~4.2.4",
        "@tailwindcss/vite": "~4.2.4",
        "eslint": "~9.39.4",
        "typescript-eslint": "~8.59.2",
        "eslint-plugin-react-hooks": "~7.1.1",
        "eslint-plugin-react-refresh": "~0.5.2",
        "eslint-plugin-jsx-a11y": "~6.10.2",
        "eslint-config-prettier": "~10.1.8",
        "prettier": "~3.8.3",
        "vitest": "~3.2.4",
        "@vitest/coverage-v8": "~3.2.4",
        "jsdom": "latest"
      }
    }
    ```

    **Step 1.2 — Create `vite.config.ts`** (copy verbatim from RESEARCH.md "Pattern 1"):

    ```typescript
    // vite.config.ts
    // Source: 01-RESEARCH.md Pattern 1; vite.dev/guide/static-deploy
    import { defineConfig } from 'vite';
    import react from '@vitejs/plugin-react';
    import tailwindcss from '@tailwindcss/vite';

    // Single source of truth for the GH Pages base path.
    // If we ever move to a custom domain, change to '/' here only.
    const BASE = '/Portfolio_Website/';

    export default defineConfig({
      base: BASE,
      plugins: [react(), tailwindcss()],
      build: {
        sourcemap: false, // OPSEC — must be false; sourcemaps leak file paths
        target: 'es2022',
      },
    });
    ```

    **Step 1.3 — Create `tsconfig.json`** (strict mode, modern target, allow JSON imports):

    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "lib": ["ES2022", "DOM", "DOM.Iterable"],
        "module": "ESNext",
        "moduleResolution": "Bundler",
        "jsx": "react-jsx",
        "strict": true,
        "noUnusedLocals": true,
        "noUnusedParameters": true,
        "noFallthroughCasesInSwitch": true,
        "esModuleInterop": true,
        "resolveJsonModule": true,
        "isolatedModules": true,
        "verbatimModuleSyntax": true,
        "skipLibCheck": true,
        "allowSyntheticDefaultImports": true,
        "useDefineForClassFields": true,
        "noEmit": true,
        "types": ["vite/client", "node"]
      },
      "include": ["src", "tests", "scripts"],
      "references": [{ "path": "./tsconfig.node.json" }]
    }
    ```

    **Step 1.4 — Create `tsconfig.node.json`** (for `vite.config.ts` typing):

    ```json
    {
      "compilerOptions": {
        "composite": true,
        "module": "ESNext",
        "moduleResolution": "Bundler",
        "target": "ES2022",
        "strict": true,
        "skipLibCheck": true,
        "allowSyntheticDefaultImports": true,
        "noEmit": true,
        "types": ["node"]
      },
      "include": ["vite.config.ts", "vitest.config.ts"]
    }
    ```

    **Step 1.5 — Create `eslint.config.js`** (copy verbatim from RESEARCH.md "Pattern 10"):

    ```javascript
    // eslint.config.js
    // Source: 01-RESEARCH.md Pattern 10
    import js from '@eslint/js';
    import tseslint from 'typescript-eslint';
    import reactHooks from 'eslint-plugin-react-hooks';
    import reactRefresh from 'eslint-plugin-react-refresh';
    import jsxA11y from 'eslint-plugin-jsx-a11y';
    import prettierConfig from 'eslint-config-prettier';

    export default tseslint.config(
      {
        ignores: ['dist/**', 'coverage/**', 'node_modules/**'],
      },
      js.configs.recommended,
      ...tseslint.configs.recommended,
      jsxA11y.flatConfigs.recommended,
      {
        files: ['**/*.{ts,tsx}'],
        plugins: {
          'react-hooks': reactHooks,
          'react-refresh': reactRefresh,
        },
        rules: {
          'react-hooks/rules-of-hooks': 'error',
          'react-hooks/exhaustive-deps': 'warn',
          'react-refresh/only-export-components': ['warn', { allowConstantExport: true }],
          'jsx-a11y/anchor-is-valid': 'error',
          'jsx-a11y/anchor-has-content': 'error',
        },
      },
      prettierConfig, // ALWAYS LAST: disable rules conflicting with Prettier
    );
    ```

    **Step 1.6 — Create `.prettierrc.json`** (verbatim from RESEARCH.md "Pattern 10"):

    ```json
    {
      "tabWidth": 2,
      "useTabs": false,
      "semi": true,
      "singleQuote": true,
      "trailingComma": "all",
      "printWidth": 100,
      "arrowParens": "always"
    }
    ```

    **Step 1.7 — Create `.prettierignore`:**

    ```
    node_modules
    dist
    coverage
    public
    *.lock
    *.min.js
    ```

    **Step 1.8 — Create `.gitignore`:**

    ```
    node_modules
    dist
    coverage
    .DS_Store
    .env
    .env.local
    .env.*.local
    .vscode/*
    !.vscode/extensions.json
    .idea
    *.log
    npm-debug.log*
    yarn-debug.log*
    yarn-error.log*
    pnpm-debug.log*
    .vite
    ```

    **Step 1.9 — Create `.editorconfig`:**

    ```
    root = true

    [*]
    indent_style = space
    indent_size = 2
    end_of_line = lf
    charset = utf-8
    trim_trailing_whitespace = true
    insert_final_newline = true

    [*.md]
    trim_trailing_whitespace = false
    ```

    **Step 1.10 — Run `npm install` to generate `package-lock.json`** and confirm dep tree resolves cleanly. If `vitest@~3.2` produces a peer-dep warning against `vite@8`, RESEARCH.md Assumptions Log A1 says to fall through to `vitest@~4.1` — bump both `vitest` and `@vitest/coverage-v8` to `~4.1` in package.json and re-run `npm install`. Do NOT use `--legacy-peer-deps` or `--force`; resolve the conflict properly.

    **Step 1.11 — Run sanity gates** (must all pass with empty src/):

    ```bash
    npx tsc --noEmit
    npx eslint .
    npx prettier --check .
    ```

    `tsc` will pass on an empty `include`. `eslint` will pass with no `.ts`/`.tsx` files matched (acceptable). `prettier --check` will pass on the configs we just authored.
  </action>

  <verify>
    <automated>npm install --no-audit --no-fund &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint . &amp;&amp; npx prettier --check . &amp;&amp; test -f package-lock.json</automated>
  </verify>

  <acceptance_criteria>
    - `test -f package.json` exits 0
    - `test -f package-lock.json` exits 0
    - `grep -F '"type": "module"' package.json` matches
    - `grep -F '"react": "~19.2' package.json` matches
    - `grep -F '"vite": "~8.0' package.json` matches
    - `grep -F '"typescript": "~5.9' package.json` matches
    - `grep -F '"tailwindcss": "~4.2' package.json` matches
    - `grep -F '"@tailwindcss/vite": "~4.2' package.json` matches
    - `grep -F '"eslint": "~9.39' package.json` matches
    - `grep -F '"@fontsource/jetbrains-mono"' package.json` matches
    - `grep -F "base: '/Portfolio_Website/'" vite.config.ts` matches
    - `grep -F 'sourcemap: false' vite.config.ts` matches
    - `grep -F 'tailwindcss()' vite.config.ts` matches
    - `grep -F 'react()' vite.config.ts` matches
    - `grep -F '"strict": true' tsconfig.json` matches
    - `grep -F 'tseslint.config' eslint.config.js` matches
    - `grep -F 'jsxA11y.flatConfigs.recommended' eslint.config.js` matches
    - `grep -F 'prettierConfig' eslint.config.js` matches (placed last)
    - `grep -F "'singleQuote': true" .prettierrc.json || grep -F '"singleQuote": true' .prettierrc.json` matches
    - `grep -F 'node_modules' .gitignore` matches
    - `grep -F 'dist' .gitignore` matches
    - `npx tsc --noEmit` exits 0
    - `npx eslint .` exits 0
    - `npx prettier --check .` exits 0
  </acceptance_criteria>

  <done>Toolchain installed and all four CI gates (typecheck, eslint, prettier, install) pass on the empty tree.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author Tailwind v4 tokens, font imports, React entrypoint, minimal index.html, App placeholder</name>
  <files>src/styles/tokens.css, src/app/main.tsx, src/app/App.tsx, index.html</files>

  <read_first>
    - .planning/phases/01-foundation-2d-recruiter-grade-shell/01-UI-SPEC.md (sections "Color → Tailwind v4 @theme token encoding" and "Typography" — eight HEX tokens locked verbatim)
    - .planning/phases/01-foundation-2d-recruiter-grade-shell/01-RESEARCH.md (sections "Pattern 2: Tailwind v4 + @theme Tokens", "Pattern 3: Self-Hosted JetBrains Mono", "Pattern 9: SEO + JSON-LD Person Schema" — Plan 06 expands the head; Plan 01 ships the minimal Vite-default head)
    - vite.config.ts (this plan's previous task — confirms base path)
  </read_first>

  <action>
    Create the React entrypoint, Tailwind v4 design tokens, and a placeholder App that compiles cleanly. Plans 04 and 05 finalize App.tsx and index.html respectively; this plan ships the smallest possible working scaffold.

    **Step 2.1 — Create `src/styles/tokens.css`** (verbatim from UI-SPEC `@theme` block + the keyframe used by Plan 04's hero cursor — declare it now so Tailwind v4 emits the rule):

    ```css
    /* src/styles/tokens.css
       Source: 01-UI-SPEC.md § Color § Tailwind v4 @theme token encoding
               01-RESEARCH.md Pattern 2 + Pattern 8 (cursor-blink keyframe) */

    @import "tailwindcss";

    @theme {
      --color-bg:        #0d1117;
      --color-fg:        #c9d1d9;
      --color-accent:    #7ee787;
      --color-warn:      #e3b341;
      --color-negative:  #ff7b72;
      --color-muted:     #8b949e;
      --color-surface-1: #161b22;
      --color-focus:     #79c0ff;

      --font-mono:       'JetBrains Mono', ui-monospace, 'SF Mono', 'Menlo', 'Consolas', monospace;
    }

    /* The single permitted Phase 1 keyframe: hero cursor block.
       Plan 04 attaches the `motion-safe:animate-cursor-blink` class to the cursor span.
       prefers-reduced-motion: reduce → keyframe never plays (Tailwind motion-safe gate). */
    @keyframes cursor-blink {
      0%, 49.9% { opacity: 1; }
      50%, 100% { opacity: 0; }
    }

    @theme {
      --animate-cursor-blink: cursor-blink 1s steps(2, end) infinite;
    }

    /* Native smooth scroll, gated by reduced-motion via Tailwind's motion-safe variant on <html>.
       The class `motion-safe:scroll-smooth` is applied in index.html. */

    /* Accessible :focus-visible default (Plan 04's components override per-element where needed). */
    :focus-visible {
      outline: 2px solid var(--color-focus);
      outline-offset: 2px;
    }
    ```

    **Step 2.2 — Create `src/app/main.tsx`** (React 19 createRoot + font CSS imports + tokens):

    ```tsx
    // src/app/main.tsx
    // Source: 01-RESEARCH.md Pattern 2 + Pattern 3
    import { StrictMode } from 'react';
    import { createRoot } from 'react-dom/client';
    import App from './App';

    // Self-hosted JetBrains Mono — only the two weights we use (per UI-SPEC: 400 + 600).
    // These imports cause Vite to bundle the woff2 files and emit @font-face rules.
    import '@fontsource/jetbrains-mono/400.css';
    import '@fontsource/jetbrains-mono/600.css';

    // Tokens + Tailwind utilities (single CSS entrypoint).
    import '../styles/tokens.css';

    const rootEl = document.getElementById('root');
    if (!rootEl) throw new Error('Root element #root not found in index.html');

    createRoot(rootEl).render(
      <StrictMode>
        <App />
      </StrictMode>,
    );
    ```

    **Step 2.3 — Create `src/app/App.tsx`** (Phase 1 placeholder; Plan 04 replaces this with the full router + shell selector. Use a body-color div so the page is visibly NOT a Vite default and the palette/font are smoke-testable.):

    ```tsx
    // src/app/App.tsx
    //
    // Phase 1 placeholder. Plan 04 replaces this with the full <App /> from
    // 01-RESEARCH.md "Code Examples → <App /> skeleton" — useQueryParams,
    // useReducedMotion, ?focus= scroll handling, isKnownPath() branching.
    //
    // Keep the export shape (default export, no props) stable so Plan 04's diff
    // is the body of the function, not the export signature.

    export default function App() {
      return (
        <main className="font-mono bg-bg text-fg min-h-screen p-8">
          <p>$ portfolio bootstrapping... (Plan 01 placeholder)</p>
        </main>
      );
    }
    ```

    **Step 2.4 — Create `index.html`** (minimal Vite entry; Plan 06 expands the head with full SEO + JSON-LD + favicon links + manifest + meta description):

    ```html
    <!doctype html>
    <html lang="en" class="motion-safe:scroll-smooth">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta name="theme-color" content="#0d1117" />
        <meta name="color-scheme" content="dark" />
        <title>Eren Atalay — Junior Cybersecurity Analyst</title>
      </head>
      <body>
        <div id="root"></div>
        <script type="module" src="/src/app/main.tsx"></script>
      </body>
    </html>
    ```

    **Step 2.5 — Build smoke test:**

    ```bash
    npm run build
    ```

    The build MUST succeed. Verify `dist/index.html` exists and `dist/assets/*.css` (the bundled Tailwind + tokens + font CSS) exists with one of the eight color tokens present in compiled form.

    **Step 2.6 — Re-run gates** (typecheck, lint, format) to confirm the new files comply.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; test -f dist/index.html &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint . &amp;&amp; npx prettier --check . &amp;&amp; grep -F '#7ee787' dist/assets/*.css</automated>
  </verify>

  <acceptance_criteria>
    - `test -f src/styles/tokens.css` exits 0
    - `test -f src/app/main.tsx` exits 0
    - `test -f src/app/App.tsx` exits 0
    - `test -f index.html` exits 0
    - `grep -F '@import "tailwindcss"' src/styles/tokens.css` matches (note: double quotes)
    - `grep -F -- '--color-bg:        #0d1117' src/styles/tokens.css` matches (UI-SPEC token verbatim)
    - `grep -F -- '--color-fg:        #c9d1d9' src/styles/tokens.css` matches
    - `grep -F -- '--color-accent:    #7ee787' src/styles/tokens.css` matches
    - `grep -F -- '--color-warn:      #e3b341' src/styles/tokens.css` matches
    - `grep -F -- '--color-negative:  #ff7b72' src/styles/tokens.css` matches
    - `grep -F -- '--color-muted:     #8b949e' src/styles/tokens.css` matches
    - `grep -F -- '--color-surface-1: #161b22' src/styles/tokens.css` matches
    - `grep -F -- '--color-focus:     #79c0ff' src/styles/tokens.css` matches
    - `grep -F "'JetBrains Mono'" src/styles/tokens.css` matches
    - `grep -F '@fontsource/jetbrains-mono/400.css' src/app/main.tsx` matches
    - `grep -F '@fontsource/jetbrains-mono/600.css' src/app/main.tsx` matches
    - `grep -F '../styles/tokens.css' src/app/main.tsx` matches
    - `grep -F 'createRoot' src/app/main.tsx` matches
    - `grep -F 'StrictMode' src/app/main.tsx` matches
    - `grep -F 'export default function App' src/app/App.tsx` matches
    - `grep -F 'lang="en"' index.html` matches
    - `grep -F 'motion-safe:scroll-smooth' index.html` matches
    - `grep -F '<div id="root"></div>' index.html` matches
    - `grep -F '/src/app/main.tsx' index.html` matches
    - `grep -F 'theme-color' index.html | grep -F '#0d1117'` matches
    - `npm run build` exits 0
    - `test -f dist/index.html` exits 0
    - `ls dist/assets/*.css | head -1` resolves to a CSS file
    - `grep -lF '#7ee787' dist/assets/*.css` matches (accent token survived in build output)
    - `npx tsc --noEmit` exits 0
    - `npx eslint .` exits 0
    - `npx prettier --check .` exits 0
  </acceptance_criteria>

  <done>Vite production build emits dist/ with Tailwind v4 tokens compiled to CSS, JetBrains Mono woff2 files bundled, and React entrypoint loading without runtime error. All four gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ GitHub Pages CDN | Untrusted browser receives static bundle over HTTPS; only static files served, no API. |
| Build host ↔ Source files | CI runs against committed source; no runtime secrets in this plan (no `.env*` references). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `vite.config.ts` `build.sourcemap` | mitigate | Set `build.sourcemap: false` (RESEARCH.md Pattern 1) — sourcemaps leak file paths and original variable names. Verified by `grep -F 'sourcemap: false' vite.config.ts` in acceptance. |
| T-01-02 | Information Disclosure | `package.json` dependency closure | mitigate | Pin all deps with tilde ranges (`~`); never use `^`. Plan 07 OPSEC checklist runs `npm audit --production`. |
| T-01-03 | Tampering | Tailwind v4 `@theme` palette drift | mitigate | Verbatim HEX tokens grep-checked in acceptance criteria; UI-SPEC color audit references these eight values as authoritative. |
| T-01-04 | Information Disclosure | `index.html` default Vite metadata | accept | Plan 01 ships minimal head; Plan 06 owns full SEO/JSON-LD with the OPSEC-gated `email` exclusion. No PII in this plan's `index.html`. |
| T-01-05 | Tampering | ESLint flat config silent no-op (RESEARCH.md Pitfall 7) | mitigate | `--max-warnings=0` is enforced via CI in Plan 02; Plan 01 keeps default exit-on-error. Plan 07 OPSEC checklist verifies lint actually catches a deliberate violation. |
| T-01-06 | Tampering | Google Fonts CDN call (privacy + 3rd-party request) | mitigate | D-08 LOCKED — `@fontsource/jetbrains-mono` self-hosted; no Google Fonts URL anywhere. Verified by acceptance grep. |
</threat_model>

<verification>
- `npm install && npm run build` succeeds end-to-end with no warnings about peer deps (or, per A1, after fall-through to vitest@~4.1).
- All four gates pass on the empty src/ tree: `npx tsc --noEmit`, `npx eslint .`, `npx prettier --check .`, `npm run build`.
- The eight UI-SPEC HEX color tokens appear verbatim in `src/styles/tokens.css`.
- The accent token (`#7ee787`) survives compilation into `dist/assets/*.css` (proves Tailwind v4 + `@theme` are wired correctly).
- `vite.config.ts` has `base: '/Portfolio_Website/'` and `sourcemap: false`.
- `src/app/main.tsx` imports the two JetBrains Mono weights via `@fontsource` (no `googleapis.com` URLs anywhere).
- ESLint flat config places `prettierConfig` last and includes `jsx-a11y` flat configs.
</verification>

<success_criteria>
The bootstrap is complete when: a fresh clone + `npm install` + `npm run build` produces a `dist/` directory with the Tailwind v4 + tokens CSS bundle, the JetBrains Mono woff2 files bundled, and a single placeholder div rendered by `<App />`. All four gates green. Plan 02 (deploy pipeline) and Plan 03 (lib utilities) can both start in Wave 2 without further bootstrap work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-2d-recruiter-grade-shell/01-01-SUMMARY.md` capturing: the exact dep versions actually resolved by `npm install` (paste the output of `npm ls react vite typescript tailwindcss eslint vitest`), whether Vitest fall-through (A1) was triggered, and any unexpected peer-dep warnings.
</output>
