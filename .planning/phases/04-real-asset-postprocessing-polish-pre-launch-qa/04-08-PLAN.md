---
phase: 04-real-asset-postprocessing-polish-pre-launch-qa
plan: 08
type: execute
wave: 4
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
  - 04-05
  - 04-06
  - 04-07
files_modified:
  - .planning/CHECKLIST-LAUNCH.md
  - .planning/CHECKLIST-OPSEC.md
  - public/assets/og-image.png
autonomous: false
requirements:
  - OPS-03
  - OPS-04
  - OPS-05
  - CTC-01
must_haves:
  truths:
    - "OG image binary at `public/assets/og-image.png` is REPLACED with a real 1200×630 PNG screenshot of the deployed text-shell hero, < 200 KB, full-resolution OPSEC-reviewed (no IPs, hostnames, employer leaks, notifications, autofill, partial reflections per RESEARCH Pitfall 4 + CHECKLIST-OPSEC § Screenshot Review)."
    - "Every CHECKLIST-OPSEC row is checked OR explicitly waived with rationale (existing checklist; Phase 4 final pass)."
    - "Every CHECKLIST-LAUNCH row is filled with real values: Lighthouse median-of-3 numbers (manual run on canonical-host deployed URL — OPS-03 source of truth per CONTEXT D-17), Gmail + Outlook delivery test results (CTC-01), iOS + Android device-QA results (OPS-04), cyber peer review verdict (CONTEXT D-15), non-cyber usability review verdict (CONTEXT D-15)."
    - "Plan 04-06 GLB outcome (Path A or Path B per timebox) is recorded in CHECKLIST-LAUNCH § 3D-08 GLB Status section."
    - "v1.0 ship-ready: zero unfilled `___` placeholders in CHECKLIST-LAUNCH; zero unchecked items in CHECKLIST-OPSEC; all CI gates green on the latest deploy."
  artifacts:
    - path: "public/assets/og-image.png"
      provides: "Real 1200×630 PNG screenshot of deployed text-shell hero, < 200 KB, OPSEC-reviewed"
    - path: ".planning/CHECKLIST-LAUNCH.md"
      provides: "Filled-in checklist with real values for every row (Lighthouse, CTC-01, peer reviews, real-device QA, GLB status)"
    - path: ".planning/CHECKLIST-OPSEC.md"
      provides: "Filled-in OPSEC final-release checkboxes"
  key_links:
    - from: "public/assets/og-image.png"
      to: "index.html (<meta property=\"og:image\">)"
      via: "<meta og:image content=\"https://{host}/Portfolio_Website/assets/og-image.png\">"
      pattern: "og-image\\.png"
acceptance_grep:
  - "test -f public/assets/og-image.png"
  - "! grep -F '___' .planning/CHECKLIST-LAUNCH.md"
  - "! grep -E '\\[ \\]' .planning/CHECKLIST-LAUNCH.md"
  - "! grep -E '\\[ \\]' .planning/CHECKLIST-OPSEC.md"
---

<objective>
Final v1.0 ship gate. This plan is human_needed — it has FOUR checkpoint tasks for human-only work that cannot be automated:

1. **OG image replacement + OPSEC review** (CONTEXT § Claude's Discretion + RESEARCH Pattern 12 + Pitfall 4) — Eren takes a real 1200×630 screenshot of the deployed text-shell hero, runs it through ImageOptim/squoosh.app to <200 KB, runs the full-resolution OPSEC review (no notifications/autofill/employer-leaks/etc), commits the binary to `public/assets/og-image.png`. exiftool CI gate strips metadata at deploy.

2. **Lighthouse median-of-3 manual run** (OPS-03 + CONTEXT D-17 + RESEARCH Pattern 13/15) — Eren runs `npx @lhci/cli@~0.15 autorun` against the deployed canonical URL (`?view=text` AND `?view=3d`); records the median of 3 runs in CHECKLIST-LAUNCH § Lighthouse. Text-shell median MUST score Performance ≥80, A11y ≥90, BP ≥90, SEO ≥90; 3D-shell is advisory.

3. **CTC-01 Gmail + Outlook delivery verification** (RESEARCH Pattern 10) — Eren submits a real test message via the deployed contact form; verifies delivery to a Gmail test inbox AND an Outlook test inbox, NOT spam, with reply-to set correctly. Records timestamps + verdict in CHECKLIST-LAUNCH § CTC-01 Web3Forms Verification.

4. **Real-device QA + Peer reviews** (OPS-04 + CONTEXT D-15 + D-16 + RESEARCH Pattern 16) — Eren commits two specific devices (one iOS mid-tier, one Android 3-4 GB RAM) and runs the 7-step protocol on each (cold-load, email reveal, contact submit, 3D mount-or-refuse, scroll, prefers-reduced-motion toggle, 30-min soak). Eren also gets ONE cyber-pro peer + ONE non-cyber usability reviewer to look at the deployed site. Records all four results in CHECKLIST-LAUNCH § Reviews + § Real-Device QA.

Output: Filled-in checklists + OG image binary swap + final SUMMARY documenting milestone-complete (or remaining gaps to v1.0 ship). This plan stays `autonomous: false` and `human_needed` until ALL fillable rows have real values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-CONTEXT.md
@.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-RESEARCH.md
@.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-UI-SPEC.md
@.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-PATTERNS.md
@.planning/CHECKLIST-OPSEC.md
@.planning/CHECKLIST-LAUNCH.md
@.github/workflows/deploy.yml
@public/assets/og-image.png
@index.html

<interfaces>
RESEARCH Pattern 12 — OG image generation (macOS native + ImageOptim path):
1. Set window to 1200px wide in Chrome/Safari (clean profile, no extensions, no notifications, no other tabs).
2. `screencapture -R 0,0,1200,630 -t png ~/Desktop/og-image.png` (force 1× DPR; verify with `sips -g pixelWidth -g pixelHeight og-image.png`).
3. `exiftool -all= -P -overwrite_original og-image.png`.
4. ImageOptim app drop OR https://squoosh.app/ — target < 200 KB.
5. Full-resolution Preview review (RESEARCH Pitfall 4 OPSEC list).
6. Place at `public/assets/og-image.png` (REPLACE).
7. Verify deployed URL: `curl -I https://{HOST}/Portfolio_Website/assets/og-image.png` → 200 OK.
8. LinkedIn DM compose + Slack DM compose preview check.

RESEARCH Pattern 13 — Lighthouse median-of-3:
```bash
# Warmup (Pitfall 8)
curl -s -o /dev/null https://{HOST}/Portfolio_Website/
curl -s -o /dev/null https://{HOST}/Portfolio_Website/?view=text
curl -s -o /dev/null https://{HOST}/Portfolio_Website/?view=3d
sleep 5
# 3 runs against canonical deployed URLs
npx @lhci/cli@~0.15 autorun
# Or run lighthouserc.json (Plan 04-07 created it)
```

RESEARCH Pattern 10 — Gmail + Outlook delivery protocol:
1. Create throwaway Gmail inbox (NOT eren.atalay@) and throwaway Outlook/Hotmail inbox.
2. Configure Web3Forms dashboard → Form Settings → Notification Email → Gmail address.
3. Submit from deployed URL with unique timestamp message body.
4. Check Inbox + Spam within 60s; record where it landed.
5. Click Reply; verify reply target = submitter's email (not Web3Forms domain).
6. Reconfigure notification email to Outlook address; resubmit; verify delivery.
7. Restore notification email to eren.atalay@.

RESEARCH Pattern 16 — Real-device QA 7-step protocol:
1. Cold-load text shell on cellular (<5s).
2. Reveal email (tap `[email (click to reveal)]`; clipboard copy may or may not work depending on browser).
3. Submit a real test message; consumes Web3Forms quota slot.
4. Open `?view=3d` shell explicitly: 3D mounts cleanly OR capability gate refuses gracefully.
5. Scroll the page inside 3D shell; verify scroll isn't trapped on Canvas (Pitfall 8 from Phase 2).
6. Toggle prefers-reduced-motion ON in OS settings; reload; verify Whoami greeting skips, camera focus is instant cut.
7. 30-minute soak with 3D shell open in foreground; verify scene runs steady OR cleanly drops to text shell + retry banner.

CONTEXT D-15 — Cyber peer review: ONE specific cyber-professional contact named at plan time (e.g. former classmate now in SOC, ex-colleague's security team contact, TryHackMe community connection). Anonymous public-sub feedback REJECTED. PLUS one non-cyber usability reviewer.

CONTEXT D-16 — Real-device QA: TWO specific committed devices. BrowserStack rejected. "Defer to v1.1" rejected.

CONTEXT D-17 — Lighthouse: deployed GH-Pages URL only; localhost rejected.
</interfaces>

<key_decisions>
- **`autonomous: false`** — every task is checkpoint:human-action OR checkpoint:human-verify. Eren performs the work; the executor (Claude) records the results into CHECKLIST-LAUNCH.md.
- **OG image OPSEC review FULL-RESOLUTION (RESEARCH Pitfall 4)** — Preview at 100% zoom, audit per the explicit list (no notifications, no extensions, no localhost, no autofill, no partial reflection, no employer NDA content). The recruiter pasting the URL into Slack/LinkedIn sees this image; defects are public.
- **Lighthouse manual median-of-3 is the OPS-03 sign-off source of truth (CONTEXT D-17)** — not the CI advisory job (Plan 04-07 — that's for build-trend artifact). Eren records the actual numbers; if text-shell median fails any threshold, this plan stays incomplete and OPS-03 can't sign off.
- **Gmail + Outlook BOTH required (CTC-01 verbatim)** — failure of either inbox to receive (or marked as spam) is a launch blocker. Mitigation paths (RESEARCH Pattern 10 step 5): verify Web3Forms account is fully verified (not trial); add `replyto`; consider hCaptcha for sender-reputation boost (defer to v1.1 unless v1.0 ship requires it).
- **Two specific devices committed (CONTEXT D-16)** — Eren names one iOS (suggest iPhone 12+ / iOS 17+) and one Android (suggest 4 GB RAM mid-tier, Pixel 6a / Galaxy A53 class) at plan-execution time. RECORDED in CHECKLIST-LAUNCH device rows. iPad optional.
- **Cyber peer reviewer NAMED (CONTEXT D-15)** — Eren names one specific contact. Anonymous public-sub feedback (e.g. /r/cybersecurity "rate my portfolio" post) is REJECTED — Pitfall 7 junior-authenticity trap.
- **Plan 04-06 GLB outcome path recorded** — Path A (real GLB shipped) or Path B (timebox expired, procedural ships, V2-3D-01 promoted). CHECKLIST-LAUNCH § 3D-08 GLB Status section captures one of the two.
- **Web3Forms account setup is PREREQUISITE** — Eren must have signed up + set notification email + set repo Variable BEFORE Task 3 (Gmail+Outlook delivery). If not done yet, do it BEFORE this plan executes; Plan 04-02 SUMMARY flagged the dependency.
- **No code changes in this plan** beyond the OG image binary swap. All work is verification + documentation.
</key_decisions>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: OG image replacement + full-resolution OPSEC review</name>
  <what-built>Plan 04-04 reconciled the `<meta property="og:image">` reference to point at `public/assets/og-image.png` on the canonical host. The binary itself has been a 1200×630 placeholder since Phase 1 Plan 01-06 (STATE.md `[Phase 1] Plan 01-06: og-image.png is a 1200x630 placeholder; Plan 07 OPSEC swaps in real terminal screenshot before live deploy`). This task swaps in the real screenshot.</what-built>
  <how-to-verify>
    Per RESEARCH Pattern 12 (verbatim):

    1. Open Chrome or Safari with a CLEAN profile (no extensions installed, no autofill, no notifications, no other tabs visible). On macOS: switch to a fresh user account if extensions can't be cleanly removed. Otherwise, run `Cmd-Shift-N` Incognito and verify no extension badges show.
    2. Set the browser window to EXACTLY 1200px wide. macOS: Window → Zoom to 1200×N (you can also use Rectangle.app or Magnet.app if installed).
    3. Navigate to the canonical deployed URL (Plan 04-04 chose this): `https://{HOST}/Portfolio_Website/?view=text`.
    4. Disable browser chrome to maximize screenshot area: hide bookmarks bar (`Cmd-Shift-B`), close DevTools.
    5. Take the screenshot at 1× DPR (NOT 2× retina). Use macOS native `screencapture`:
       ```bash
       screencapture -R 0,0,1200,630 -t png ~/Desktop/og-image.png
       ```
       Verify dimensions: `sips -g pixelWidth -g pixelHeight ~/Desktop/og-image.png` → must be exactly 1200×630. If 2400×1260 (2× DPR), retake using Chrome DevTools "Capture screenshot" command at custom 1200×630 viewport.
    6. Strip metadata: `exiftool -all= -P -overwrite_original ~/Desktop/og-image.png`.
    7. Compress: drop into ImageOptim.app OR upload to https://squoosh.app/ (PNG → OxiPNG / pngquant Lossy 60-80 quality). Target file size: < 200 KB. Verify: `du -h ~/Desktop/og-image.png` → < 200K.
    8. **FULL-RESOLUTION OPSEC REVIEW** (RESEARCH Pitfall 4 + CHECKLIST-OPSEC § Screenshot Review): open in Preview at 100% zoom (Cmd-0). Audit checklist:
       - [ ] No notification banners (Slack, Discord, Mail, Calendar, system) anywhere in frame.
       - [ ] No browser extension badges in the address bar OR right of URL.
       - [ ] No password-manager autofill suggestions OR previously-typed strings in URL bar.
       - [ ] NOT `localhost:` or `127.0.0.1` in the address bar — must be the canonical deployed URL.
       - [ ] No partial reflection of face / room / family member visible in any reflective UI element (rare but possible if Eren has dark-mode UI showing window reflections).
       - [ ] No browser tab title leaking another site Eren has open in another window.
       - [ ] No employer-NDA-covered content (logos in any reflection, employer-issued machine names, internal hostnames).
       - [ ] Title text reads `Eren Atalay — Junior Cybersecurity Analyst (UK)` correctly + the 4 link strip is visible.
    9. Place at `public/assets/og-image.png` (REPLACE existing placeholder): `cp ~/Desktop/og-image.png public/assets/og-image.png`. Commit: `git add public/assets/og-image.png && git commit -m "feat(04-08): replace placeholder OG image with deployed text-shell screenshot"`.
    10. Verify deployed survival after deploy: `curl -I https://{HOST}/Portfolio_Website/assets/og-image.png` → `HTTP/2 200` + `content-type: image/png`.
    11. Verify visual previews (manual): paste the deployed URL into LinkedIn DM compose AND Slack DM compose AND `https://www.opengraph.xyz/url/<encoded-url>` → preview cards render the new screenshot, not a broken-image / generic Vite default / GitHub default.
    12. Tick the OG image rows in CHECKLIST-LAUNCH § SEO + Sharing.
  </how-to-verify>
  <resume-signal>Type "og-image-shipped" once the binary is committed AND deployed AND previews confirmed in LinkedIn + Slack. Or describe any review failure.</resume-signal>

  <action>
    Present the checkpoint context above to the user. Wait for the resume signal. Once received, proceed to the next task in this plan (or, if this is the terminal task, run `gsd-sdk query commit` and produce SUMMARY.md per the &lt;output&gt; section).
  </action>
  <verify>
    <automated>echo "Checkpoint task — verification is the user's resume signal: Type "og-image-shipped" once the binary is committed AND deployed AND previews c..." &amp;&amp; true</automated>
  </verify>
  <done>
    User has typed the resume signal documented in &lt;resume-signal&gt; above. Decision / verification recorded in SUMMARY.md at plan completion.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Lighthouse median-of-3 manual run on deployed canonical URL</name>
  <what-built>Plan 04-07 added `lighthouserc.json` + advisory CI lighthouse job. CONTEXT D-17 explicit: "manual median-of-3 is the OPS-03 sign-off source of truth"; CI is advisory. This task is Eren's authoritative manual run.</what-built>
  <how-to-verify>
    Per RESEARCH Pattern 13 + Pattern 15 + Pitfall 8 (verbatim):

    1. Make sure the latest deploy from Plan 04-07's CI is live + the OG image from Task 1 is deployed (Vercel/Pages may take 1-2 min after push).
    2. Warmup (Pitfall 8 — CDN cold cache distorts first-run scores):
       ```bash
       curl -s -o /dev/null https://{HOST}/Portfolio_Website/
       curl -s -o /dev/null "https://{HOST}/Portfolio_Website/?view=text"
       curl -s -o /dev/null "https://{HOST}/Portfolio_Website/?view=3d"
       sleep 5
       ```
    3. Run lhci using the committed `lighthouserc.json` (Plan 04-07):
       ```bash
       npx @lhci/cli@~0.15 autorun
       ```
       OR equivalent CLI (uses the same config implicitly):
       ```bash
       npx @lhci/cli@~0.15 autorun \
         --collect.url=https://{HOST}/Portfolio_Website/?view=text \
         --collect.url=https://{HOST}/Portfolio_Website/?view=3d \
         --collect.numberOfRuns=3 \
         --upload.target=temporary-public-storage \
         --assert.preset=lighthouse:no-pwa
       ```
    4. The CLI will run 3 audits per URL (6 total), upload to temporary-public-storage (free, public links), and print median-vs-threshold assertions.
    5. Capture the actual numbers from the CLI output (look for `Median run` table). Each category is a 0..1 float; multiply by 100 for the score.
    6. Fill in CHECKLIST-LAUNCH § Lighthouse rows verbatim. For text shell:
       - Run 1: Perf=__, A11y=__, BP=__, SEO=__
       - Run 2: ...
       - Run 3: ...
       - Median: ...
       - Verdict: PASS (if all four medians ≥ thresholds) / FAIL (any below)
    7. **If text shell median FAILS any threshold:** this is a v1.0 ship blocker. Investigate:
       - Performance: check the lhci HTML report; common offenders are cold-cache LCP, render-blocking resources, large JS chunks. Phase 2 OPS-02 budgets predicted ≤ 120 KB gz text shell + ≤ 450 KB gz 3D chunk; if entry chunk grew past budget, Plan 04-01 + 04-05 size-limit gates would have caught it locally — this is unexpected.
       - A11y: should be 95+ if Phase 1 WCAG palette + semantic HTML survived. Check for missing alt attributes, low-contrast text, unlabeled form inputs.
       - BP: should be 95+ if CSP + sourcemap-off + sec-headers all in place.
       - SEO: should be 95+ if `<title>`, meta description, JSON-LD, sitemap all present.
       - Fix → redeploy → re-run.
    8. 3D shell is advisory (CONTEXT D-17). Record numbers but no PASS/FAIL gate. Verify: page does NOT throw a Lighthouse error and does NOT render blank.
    9. The temporary-public-storage URLs from the CLI output go in the SUMMARY for trend reference.
    10. Tick all Lighthouse rows in CHECKLIST-LAUNCH § Lighthouse on Deployed URL.
  </how-to-verify>
  <resume-signal>Type "lighthouse-ok" once text-shell median PASSES all four thresholds AND 3D-shell rows are documented (advisory). If any text-shell threshold FAILED, describe the failure + remediation plan.</resume-signal>

  <action>
    Present the checkpoint context above to the user. Wait for the resume signal. Once received, proceed to the next task in this plan (or, if this is the terminal task, run `gsd-sdk query commit` and produce SUMMARY.md per the &lt;output&gt; section).
  </action>
  <verify>
    <automated>echo "Checkpoint task — verification is the user's resume signal: Type "lighthouse-ok" once text-shell median PASSES all four thresholds AND 3D-sh..." &amp;&amp; true</automated>
  </verify>
  <done>
    User has typed the resume signal documented in &lt;resume-signal&gt; above. Decision / verification recorded in SUMMARY.md at plan completion.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: CTC-01 Gmail + Outlook end-to-end delivery verification</name>
  <what-built>Plan 04-02 wired the Web3Forms POST + UI; Plan 04-05 mounted in 3D shell; Plan 04-07 + 04-04 verified CSP allows the cross-origin POST. This task verifies the END-TO-END path: a stranger's submission reaches Eren's actual notification inbox in BOTH Gmail and Outlook, NOT spam, with reply-to working.</what-built>
  <how-to-verify>
    Per RESEARCH Pattern 10 (verbatim):

    1. Web3Forms account setup PREREQUISITE (if not done):
       - Sign up at https://web3forms.com.
       - Create a form via Forms → New form. Web3Forms issues an access key (public-by-design).
       - Set Notification Email to a TEST GMAIL inbox first (not `eren.atalay@…` — sending to your own inbox biases spam filters).
       - Set repo Variable: GitHub Settings → Secrets and variables → Actions → Variables → `VITE_WEB3FORMS_KEY=<key>`.
       - Trigger redeploy so the build picks up the env var.
    2. **Gmail test:**
       a. Open the deployed contact form: `https://{HOST}/Portfolio_Website/#contact`.
       b. Submit with a unique message body, e.g. `Test submission 2026-MM-DD HH:MM — Gmail inbox check`. Use `recruiter@example.com` (or any throwaway external-looking email; NOT eren.atalay@).
       c. Expect inline `$ message_sent` UI within 5s.
       d. Check the test Gmail inbox WITHIN 60 seconds — both Inbox AND Spam folders.
       e. Record in CHECKLIST-LAUNCH § CTC-01:
          - Date/time: 2026-MM-DD HH:MM
          - Inbox / Spam: <where it landed>
          - Reply-to set correctly: <yes/no — click Reply in Gmail; verify the To: field shows the submitter's email, NOT a Web3Forms-domain email>
          - Subject line: <verbatim — should be `[Portfolio enquiry] from <Submitter Name>`>
       f. **If Spam:** investigate per RESEARCH Pattern 10 step 5 — check Web3Forms From address; verify account fully verified; consider hCaptcha (defer to v1.1 unless this blocks v1.0).
    3. **Outlook/Hotmail test:**
       a. In Web3Forms dashboard, change Notification Email to a TEST OUTLOOK/HOTMAIL inbox.
       b. Submit again with a different unique timestamp.
       c. Check the Outlook inbox WITHIN 60 seconds — Inbox + Junk Email.
       d. Record same fields in CHECKLIST-LAUNCH (the second row).
    4. **Honeypot manual test:**
       a. Open DevTools on the deployed form; find the `<input name="botcheck">` (style display:none).
       b. Set its value: `document.querySelector('input[name="botcheck"]').value = 'i-am-a-bot'`.
       c. Submit the form.
       d. Verify: the success UI shows BUT no submission arrives in the configured notification inbox (check inbox is empty for the next 5 min).
       e. Tick the honeypot row in CHECKLIST-LAUNCH.
    5. **Restore production state:**
       a. Set Web3Forms Notification Email back to `eren.atalay@…` (the real inbox Eren wants production submissions in).
       b. Verify Web3Forms dashboard quota: should be at ~3 submissions used out of 250 (Gmail test + Outlook test + honeypot test). 247/month remaining for actual recruiters.
    6. Tick all CTC-01 rows in CHECKLIST-LAUNCH § CTC-01 Web3Forms Verification.
  </how-to-verify>
  <resume-signal>Type "ctc-01-verified" once both inboxes received messages NOT in spam AND honeypot abort tested AND notification email restored to eren.atalay@. If either inbox failed, describe the failure + escalation (hCaptcha integration deferred to v1.1 if unsolvable; v1.0 may ship with single-inbox verification + monitor).</resume-signal>

  <action>
    Present the checkpoint context above to the user. Wait for the resume signal. Once received, proceed to the next task in this plan (or, if this is the terminal task, run `gsd-sdk query commit` and produce SUMMARY.md per the &lt;output&gt; section).
  </action>
  <verify>
    <automated>echo "Checkpoint task — verification is the user's resume signal: Type "ctc-01-verified" once both inboxes received messages NOT in spam AND honey..." &amp;&amp; true</automated>
  </verify>
  <done>
    User has typed the resume signal documented in &lt;resume-signal&gt; above. Decision / verification recorded in SUMMARY.md at plan completion.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Real-device QA (iOS + Android) + Peer reviews (cyber + non-cyber) + final OPSEC sweep</name>
  <what-built>Plans 04-01..04-07 delivered the v1.0 functional surface. Per CONTEXT D-15 + D-16 + RESEARCH Pattern 16 + Pattern 17, the final ship gate is human eyeballs on real hardware + named human reviewers. BrowserStack rejected (CONTEXT D-16). Anonymous public-sub feedback rejected (CONTEXT D-15 — Pitfall 7 trap).</what-built>
  <how-to-verify>
    **Step 1 — Real-device QA (OPS-04 + CONTEXT D-16):**

    Eren commits two specific devices. Suggested:
    - iOS: iPhone 12+ / iOS 17+ (3 GB RAM tier, 4-year-old class — recruiter-realistic).
    - Android: Pixel 6a OR Galaxy A53 (4 GB RAM mid-tier, Chrome 130+, Android 13+).
    - Optional: iPad if available (Phase 2 D-01 routes iPads to 3D shell; useful 3rd row in checklist).

    Per-device 7-step protocol (RESEARCH Pattern 16):
    1. Cold-load text shell on cellular (NOT wi-fi) — recruiter-realistic. Time-to-hero-text-visible < 5s.
    2. Reveal email — tap `[email (click to reveal)]`; clipboard copy may or may not work depending on iOS/Android browser permissions; either is OK as long as the visible address appears.
    3. Submit a real test message via the contact form. Inline `$ message_sent` block replaces form. Eren sees the message in his real notification inbox.
    4. Open `?view=3d` shell explicitly (URL-bar): EITHER (a) 3D scene mounts cleanly, OR (b) capability gate refuses gracefully (text shell with explanation banner). Both are PASS; only blank canvas / WebGL error / silent freeze is FAIL.
    5. Scroll inside 3D shell: page scrolls normally OR (preferred) page is non-scrolling and canvas captures gestures correctly. Phase 2 D-04 contract.
    6. Toggle prefers-reduced-motion ON in OS settings (iOS: Settings → Accessibility → Motion → Reduce Motion ON; Android: Settings → Accessibility → Remove animations ON); reload. WhoamiGreeting skips to final state instantly. Camera focus is instant cut.
    7. 30-minute soak — leave 3D shell open in foreground. iOS Safari has aggressive WebGL memory limits; Phase 2 D-13 + D-14 should swap to text shell with [retry 3D] banner if context lost. PASS = scene runs steady OR cleanly drops to text shell + retry banner. FAIL = silent black screen.

    Record in CHECKLIST-LAUNCH § Real-Device QA (iOS row + Android row, optional iPad row): Model, OS version, Test date, Text shell PASS/FAIL, 3D shell PASS/FAIL/REFUSED-GRACEFULLY, Notes (any per-step issues).

    **Step 2 — Cyber peer review (CONTEXT D-15):**

    Eren names ONE specific cyber-professional contact (former classmate now in SOC, ex-colleague's security team contact, TryHackMe community connection). Anonymous public-sub feedback REJECTED.

    Send the deployed URL to the named reviewer. Ask for:
    - "this person actually gets this field" gut-check (PROJECT.md core value).
    - Pitfall sweep: any cyber-cliché (Matrix code rain, padlocks, hooded hackers, fake REPL, padded skill bars, "Ethical Hacker" self-titling) — should be NONE in v1.0.
    - Provenance check: are skills in the skills tag list backed by projects/write-ups (CNT-03 + Pitfall 7).
    - OPSEC sweep: any leaked details (employer terms, internal IPs/hostnames, partial face/room reflection in screenshots, NDA-covered content).

    Record in CHECKLIST-LAUNCH § Reviews § Cyber peer: Reviewer (name or anonymized ID), Date, Verdict (PASS / RESERVATIONS / FAIL), Notes.

    **Step 3 — Non-cyber usability review (CONTEXT D-15):**

    Eren names ONE non-cyber reviewer (e.g. a recruiter contact, a designer friend, a non-technical relative). Ask for:
    - First-impression speed: under 2 seconds from landing, can they find the CV link? GitHub link? LinkedIn? Email?
    - Visual readability: any text hard to read? Any UI element confusingly placed?
    - 3D-shell experience: does the click-to-focus camera animation feel natural? Is the contact form reachable inside the 3D shell?

    Record in CHECKLIST-LAUNCH § Reviews § Non-cyber: same fields.

    **Step 4 — Final OPSEC sweep (CHECKLIST-OPSEC.md):**

    Walk through every row in `.planning/CHECKLIST-OPSEC.md` (Phase 1 + 3 extended). Tick each row OR document the waiver. Specific Phase 4 attention:
    - Asset Metadata: exiftool CI gate runs on every release (deploy.yml lines 60-104) — confirm the latest deploy log shows the gate as PASS. CV PDF: open in Preview, check Document Properties.
    - Screenshot Review: full-resolution audit of every PNG in `public/assets/writeups/<slug>/` (Phase 3 write-up screenshots). Phase 4 OG image audit done in Task 1.
    - Source / Bundle: `npm audit --production` runs in CI (Plan 04-07 gate); tick that row. `grep -rE "console\.(log|debug)" dist/assets/` returns nothing — Plan 04-07 dev-helper-strip gate verifies.
    - External Links: Plan 04-07 CI gate audits.
    - Personal Information: confirm no home address, phone, full DOB anywhere in source/dist.
    - Email + Contact: confirm grep `dist/index.html` for actual TLD returns nothing matching the plaintext email (only obfuscated `ENCODED_EMAIL` string).
    - SEO + Meta: title/description/og:image/JSON-LD all already verified in Plan 04-04 + Task 1.
    - TXT-06 parity: `npm run parity` in CI; manual section parity walk; manual provenance check.

    Record in CHECKLIST-OPSEC: tick every row. Any row that CAN'T be ticked (e.g. "screen reader test on actual NVDA/JAWS") is documented as a v1.1 deferral.

    **Step 5 — Plan 04-06 GLB outcome (Path A or Path B):**

    Read Plan 04-06 SUMMARY.md. Either:
    - Path A: real GLB shipped. Record in CHECKLIST-LAUNCH § 3D-08 GLB Status: total size, CC0 sources, visual emissive bloom confirmed (verified in real-device QA Step 1 / iOS + Android implicit visual verification of postprocessing + emissive).
    - Path B: timebox expired. Record: "v1.1 V2-3D-01 promotion; procedural workstation ships in v1.0; 3D-08 PARTIAL".
  </how-to-verify>
  <resume-signal>Type "shipped" once: BOTH device rows have real fill values + PASS verdicts; cyber peer review has a named reviewer + verdict; non-cyber usability review has a named reviewer + verdict; CHECKLIST-OPSEC has zero unchecked rows (or documented waivers); 3D-08 GLB Status row reflects Plan 04-06 outcome. Or describe any FAIL that blocks v1.0 ship.</resume-signal>

  <action>
    Present the checkpoint context above to the user. Wait for the resume signal. Once received, proceed to the next task in this plan (or, if this is the terminal task, run `gsd-sdk query commit` and produce SUMMARY.md per the &lt;output&gt; section).
  </action>
  <verify>
    <automated>echo "Checkpoint task — verification is the user's resume signal: Type "shipped" once: BOTH device rows have real fill values + PASS verdicts; cyb..." &amp;&amp; true</automated>
  </verify>
  <done>
    User has typed the resume signal documented in &lt;resume-signal&gt; above. Decision / verification recorded in SUMMARY.md at plan completion.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author ↔ deployed bundle | Final pre-public-launch sweep — last point to catch leaks before recruiter visibility |
| Author ↔ named peer reviewer | Trusted contact; verdict shapes ship/no-ship decision |
| Author ↔ real device | Cellular network + real OS WebGL2 quirks not surfaceable in DevTools (Pitfall 5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Information Disclosure | OG image leaks notification banner / extension badge / partial face / employer NDA on first LinkedIn post | mitigate | Task 1 full-resolution Preview review with explicit checklist; OPSEC review BEFORE commit. exiftool CI gate strips metadata. |
| T-04-08-02 | Denial of Service | iOS Safari WebGL2 memory pressure causes context loss within minutes; recruiter sees frozen scene | mitigate | Task 4 Step 1 # 7 (30-min soak) catches this; Phase 2 D-13 webglcontextlost listener swaps to text shell with retry banner. |
| T-04-08-03 | Information Disclosure | Plaintext email leak survives Plan 04-07 CI gate via unanticipated path (e.g. inline string in a chunk that grep misses) | mitigate | Task 4 Step 4 manual `grep -rE '<actual TLD pattern>' dist/` audit (not in CI because CI doesn't know the plaintext). |
| T-04-08-04 | Repudiation | Eren self-certifies device QA without actually testing on real hardware (Pitfall 5) | accept | Plan structure forces named device + date in CHECKLIST-LAUNCH; CONTEXT D-16 explicit rejection of DevTools emulation. Spirit of the gate relies on Eren's integrity. |
| T-04-08-05 | Information Disclosure | Cyber peer reviewer takes screenshots and posts publicly | accept | Eren chooses a TRUSTED named reviewer (CONTEXT D-15 explicit rejection of public-sub feedback); reviewer is professional contact, not anonymous internet. |
| T-04-08-06 | Tampering | Spam burst exhausts Web3Forms 250/mo quota in week 1 | mitigate | Plan 04-02 honeypot abort saves slots; Eren monitors dashboard week 1; if quota burns abnormally, add hCaptcha (v1.1) per Pitfall 7. |
| T-04-08-07 | Misconfiguration | Lighthouse CI advisory passes BUT manual median FAILS due to DPR / preset / device drift | accept | CONTEXT D-17 explicit: manual is the source of truth; CI is advisory. If manual fails, that IS the v1.0 ship blocker. |
</threat_model>

<verification>
**Per-task verification:** see each task's `<resume-signal>` — Eren's signal IS the verification.

**Plan-level verification:**
1. CHECKLIST-LAUNCH.md has zero `___` placeholders (all fillable rows filled).
2. CHECKLIST-LAUNCH.md has zero `[ ]` (unchecked) checkboxes — all ticked.
3. CHECKLIST-OPSEC.md has zero `[ ]` (unchecked) checkboxes — all ticked OR documented as waivers.
4. `public/assets/og-image.png` exists, is 1200×630, < 200 KB, OPSEC-reviewed.
5. Plan 04-06 GLB outcome row reflects actual outcome.

**Until Eren signals "shipped" on Task 4, this plan stays `human_needed`.**
</verification>

<success_criteria>
- OG image binary swapped + OPSEC-reviewed + deployed + LinkedIn/Slack preview verified.
- Lighthouse manual median-of-3 on text shell PASSES Performance ≥80, A11y ≥90, BP ≥90, SEO ≥90 (CONTEXT D-17 OPS-03 sign-off).
- CTC-01 Gmail + Outlook delivery verified, NOT spam, reply-to correct (CONTEXT D-15 + RESEARCH Pattern 10).
- Two named real devices (iOS + Android) — 7-step QA protocol PASS on each (CONTEXT D-16 + RESEARCH Pattern 16).
- One named cyber peer reviewer — verdict recorded (CONTEXT D-15).
- One named non-cyber usability reviewer — verdict recorded (CONTEXT D-15).
- CHECKLIST-OPSEC final-pass complete; zero unchecked rows.
- Plan 04-06 GLB outcome (Path A or Path B) reflected in CHECKLIST-LAUNCH § 3D-08 GLB Status.
- v1.0 milestone-complete.
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-08-SUMMARY.md` documenting:
- Each Task's outcome (Eren's resume-signal text + any deltas).
- Final Lighthouse median scores (text shell + 3D shell).
- Final Web3Forms verification: Gmail + Outlook verdicts, honeypot manual test, restored notification email.
- Real-device QA verdicts (iOS model + version + verdicts; Android model + version + verdicts).
- Peer review verdicts (cyber + non-cyber — names redacted to "[Cyber peer]" / "[Usability peer]" if Eren prefers privacy; verbatim verdict + key feedback bullets).
- Plan 04-06 GLB outcome integration (Path A vs Path B; if Path B, link to V2-3D-01 in v1.1 milestone planning).
- Any v1.1 deferrals identified during sweep (each with rationale + REQUIREMENTS.md V2-* row reference).
- Final v1.0 ship verdict: SHIPPED / SHIP-WITH-CAVEATS / BLOCKED (with caveats listed for the middle case + remediation plan for the last case).
- Update STATE.md milestone status to "milestone complete" if shipped.
- Update ROADMAP.md Phase 4 status: `[x]` checkmark.
</output>
