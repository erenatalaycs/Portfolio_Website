---
context: phase
phase: 04-real-asset-postprocessing-polish-pre-launch-qa
task: 1
total_tasks: 8
status: in_progress
last_updated: 2026-05-08T13:31:00.411Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below is a hard rule discovered earlier in the project (some by failure, some by saved policy).

- [ ] CONSTRAINT: **No fabricated lab evidence (Plan 03-06)** — saved feedback memory says refuse fake CTF/Detection/CTI write-ups; Plan 03-06 stays deferred indefinitely. Phase 4 plans MUST NOT touch `src/content/writeups/*.mdx`. If autonomous mode lists Phase 3 incomplete, pass `--from 4`.
- [ ] CONSTRAINT: **RESEARCH overrides CONTEXT** for postprocessing API. CONTEXT D-08 wrote `threshold` for Bloom — the correct prop is `luminanceThreshold`. CONTEXT D-05 wrote `bounds` as static array — the correct shape is `bounds={() => [30, 50]}`. CONTEXT D-02 said KTX2 textures — actually WebP (gltfjsx default; KTX2 unreachable without forbidden tooling).
- [ ] CONSTRAINT: **CSP must extend `connect-src` to include `https://api.web3forms.com`** in `index.html` — without this the contact form silently fails in production (RESEARCH Pitfall 3). Must also add `form-action` if the policy is restrictive.
- [ ] CONSTRAINT: **Plan 04-08 is `autonomous: false`** — its 4 tasks (OG image swap, Lighthouse manual median-of-3, Web3Forms Gmail+Outlook delivery test, real-device QA on iOS+Android) fundamentally need human action. Do not let any agent fabricate sign-off. Verification stays `human_needed` until Eren records results in `CHECKLIST-LAUNCH.md`.

**Do not proceed until all four boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Fabricated lab write-ups | Generating MDX content for unrun labs (Splunk+Sigma rule, MalwareBazaar triage, PortSwigger JWT) under Eren's real public name. Career-damaging in cyber community. | blocking | Plan 03-06 stays deferred. Phase 4 plans explicitly skip `src/content/writeups/*.mdx`. Autonomous mode invoked with `--from 4`. |
| Wrong postprocessing prop names | `threshold` instead of `luminanceThreshold` on Bloom; static array on `<PerformanceMonitor>` `bounds`; scalar on ChromaticAberration `offset`. Silently no-effect at runtime. | blocking | RESEARCH.md Pattern 5 documents the correct names; plans 04-01 use them verbatim; tests assert `<PostFX />` mounts without warnings. |
| CSP without web3forms domain | Phase 1 baseline CSP has `connect-src 'self'` only; Web3Forms POST is cross-origin and fails silently in production. | blocking | Plan 04-02 adds `https://api.web3forms.com` to `connect-src` (and `form-action` if needed); CI gate in 04-07 audits this. |
| Plan 04-02 + 04-03 same-file race | Both modify `src/sections/Contact.tsx`; without dependency edge, executor parallelizes and the second writer overwrites. | blocking | 04-03 frontmatter `depends_on: ['04-02']` — sequences correctly. |

<current_state>
Mid-Wave-1 of Phase 4 autonomous run. UI-SPEC + RESEARCH + PATTERNS + 8 PLANs all committed (HEAD: `dbfc4f9`). Plan-checker found 3 BLOCKERs which were fixed inline. Wave 1 launched two background gsd-executor agents in worktrees:

- **04-01 postprocessing** — agent ID `a3b468582af702b26`, status unknown at pause
- **04-02 ContactForm** — agent ID `a1a751d8f89e3b8e9`, status unknown at pause

Both will continue running; check their commits on resume.

Held back from Wave 1 launch:
- **04-03 LiveProfiles** — needs 04-02 to commit Contact.tsx first
- **04-04 SEO surfaces** — Task 1 is `checkpoint:decision` (host pick) — better to surface to Eren alongside the 04-06 GLB decision than launch and pause immediately

Remaining waves all have at least one human checkpoint, and Wave 4 (04-08) is entirely human-needed.
</current_state>

<completed_work>

- ✓ 04-CONTEXT.md (smart discuss, 17 decisions D-01..D-17) — `ad24f8f`
- ✓ 04-UI-SPEC.md (verified APPROVED, 3 PASS / 3 FLAG / 0 BLOCK) — `52f175c`
- ✓ 04-RESEARCH.md (with API corrections + open Qs RESOLVED) — `d739377` + `dbfc4f9`
- ✓ 04-PATTERNS.md (codebase analogs for 20 files) — `101c6f7`
- ✓ 8 PLAN.md files (04-01..04-08, 4 waves) — `101c6f7`
- ✓ Plan-checker blocker fixes — `dbfc4f9`
- ⧗ Plan 04-01 execute — background agent running (a3b468582af702b26)
- ⧗ Plan 04-02 execute — background agent running (a1a751d8f89e3b8e9)
</completed_work>

<remaining_work>

- Plan 04-03 LiveProfiles — sequential after 04-02 (same-file dep)
- Plan 04-04 SEO surfaces — checkpoint at Task 1 (host pick)
- Plan 04-05 ContactForm + LiveProfilesShortcut mount in 3D shell
- Plan 04-06 Real CC0 GLB — checkpoint at Task 1 (path + 7-day timebox + D-02 ack)
- Plan 04-07 CHECKLIST-LAUNCH + lighthouserc + 5 CI gates
- Plan 04-08 Pre-launch sign-off — autonomous: false, entirely human-needed
- /gsd-code-review 4 (autonomous workflow step 3c.5)
- Phase 4 verification routing
- /gsd-ui-review 4 (advisory)
- Milestone v1.0 lifecycle: audit → complete → cleanup
</remaining_work>

<decisions_made>

- Autonomous mode invoked with implicit `--from 4` semantics (skip Plan 03-06 per no-fabricated-lab-evidence rule)
- All 4 grey areas accepted as recommended; CONTEXT D-01..D-17 captured
- WebP textures locked (overrides CONTEXT D-02 KTX2 — gltfjsx tool reality)
- Plan 04-03 depends_on: ['04-02'] (dependency edge encodes same-file sequencing)
- Plan 04-08 stays autonomous: false (human-needed verification gates correct)
- Wave 1 launched with 04-01 + 04-02 only (held back 04-04 + 04-03 for sequencing/checkpoint reasons)
</decisions_made>

<blockers>

- **04-04 host pick (Eren input):** `eren-atalay.github.io` (CONTEXT D-17) vs `erenatalaycs.github.io` (currently in `index.html` + `public/sitemap.xml`). Whichever wins propagates to canonical link, og:url, og:image, JSON-LD url+image, sitemap.xml, robots.txt, lighthouserc.json.
- **04-06 GLB path + timebox + D-02 ack (Eren input):** option-a Poly Haven composite (recommended) / option-b Quaternius / option-c Base Mesh / option-d skip-to-v1.1. AND ISO timebox start date. AND "D-02 KTX2→WebP correction acknowledged" confirmation.
- **04-08 entirely human (Eren input):** 4 checkpoint:human-action tasks blocking ship.
- **External prerequisites for Phase 4 ship** (8 items in `human_actions_pending` of HANDOFF.json) — Web3Forms account, host pick, TryHackMe + HackTheBox handles, GLB choice, two real devices, named peer reviewers, OG screenshot, Gmail + Outlook test inboxes.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured handoff state, completed/remaining tasks, blockers, background agents
2. `.planning/STATE.md` — last session, project reference, decisions
3. This file (`.continue-here.md`) — blocking constraints + current state
4. `.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-CONTEXT.md` — D-01..D-17 locked decisions
5. `.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-RESEARCH.md` — API corrections override CONTEXT (look at "RESOLVED" Open Questions section)
6. `.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-UI-SPEC.md` — copy locked verbatim
7. `.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-PATTERNS.md` — file-by-file analogs

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN: Fabricating lab write-ups** → Plan 03-06 stays deferred; Phase 4 plans skip `src/content/writeups/*.mdx`.
- **ANTI-PATTERN: Wrong postprocessing prop names** → use the verified RESEARCH names (`luminanceThreshold`, function-shaped `bounds`, tuple `offset`, explicit `multisampling={0}`).
- **ANTI-PATTERN: Forgetting CSP `connect-src` extension** → Plan 04-02 adds `api.web3forms.com`; Plan 04-07 CI gate audits.
- **ANTI-PATTERN: Same-file Wave 1 race** → Plan 04-03 already depends_on Plan 04-02 in frontmatter.

## Infrastructure State

- **Background agents:** `a3b468582af702b26` (Plan 04-01) + `a1a751d8f89e3b8e9` (Plan 04-02). Will continue running in worktrees regardless of pause. Check `git log --oneline -20` for `feat(04-01)` / `feat(04-02)` commits + presence of `04-01-SUMMARY.md` / `04-02-SUMMARY.md` to detect completion.
- **Working tree:** mostly clean; `.planning/STATE.md` and `.planning/config.json` modified but uncommitted (state-record-session updates + `_auto_chain_active` toggle).
- **Branch:** `main` (no per-phase branch per workflow.use_worktrees: true; agents work in isolated worktrees that auto-merge when they commit).
- **No running dev server / no watchers.**

<context>
We're in /gsd-autonomous mid-Wave-1. The autonomous workflow naturally pauses at human checkpoints. The two running executor agents will likely commit their work over the next 10-30 min — when resuming, the FIRST step is detecting whether they finished, then deciding next-wave dispatch.

Eren has a list of 8 real-world prerequisites to gather (web3forms account, GH-Pages host pick, two physical devices, named peer reviewers, OG screenshot, Gmail+Outlook test inboxes, TryHackMe+HackTheBox handles, GLB asset choice) — these are the bottleneck for shipping v1.0, not agent throughput.
</context>

<next_action>
On resume:

1. **Check background agent completion FIRST:**
   ```bash
   git log --oneline -20
   ls .planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-01-SUMMARY.md .planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-02-SUMMARY.md 2>/dev/null
   ```
   If both SUMMARYs exist + commits land → Wave 1 (autonomous portion) done. Mark task #4 partial.
   If one or both missing → re-spawn or wait via Monitor on agent IDs in HANDOFF.json `background_agents`.

2. **Surface the human-decision batch to Eren** (do not run plans yet):
   - 04-04 host pick (`eren-atalay.github.io` vs `erenatalaycs.github.io`)
   - 04-06 GLB path + ISO timebox start date + D-02 KTX2→WebP ack
   - TryHackMe + HackTheBox handles (or "omit both" if not active)
   - Confirmation that web3forms.com signup is in progress

3. **Once Eren responds**, dispatch:
   - 04-03 LiveProfiles (now that 04-02 has committed Contact.tsx)
   - 04-04 SEO with the chosen host
   - 04-05 (depends on 04-01..04-03)
   - 04-06 if GLB path picked AND timebox not skip-to-v1.1
   - 04-07 (depends on all prior)

4. **Plan 04-08 stays last** — only attempt when Eren confirms all 8 external prerequisites are in hand.

5. **Then route through autonomous step 3c.5** (`/gsd-code-review 4`) → step 3d (verification) → step 3d.5 (`/gsd-ui-review 4`) → step 5 (milestone lifecycle: audit → complete → cleanup).

Do NOT touch Plan 03-06.
</next_action>
