---
context: phase
phase: 04-real-asset-postprocessing-polish-pre-launch-qa
plan: post-04-08-hs-redesign
plan_path: /Users/erenatalay/.claude/plans/neon-tabbing-workstation.md
task: 0
total_tasks: 8
status: paused
last_updated: 2026-05-13T15:01:15.856Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was discovered through failure in a previous session.
> Acknowledge each one explicitly before proceeding to any code change.

- [ ] CONSTRAINT: **drei `<Html transform distanceFactor>` is empirical, not spec.** The 1.5–2.5 range in Phase 2/3 spec is theoretical and never deploy-verified. Real behaviour: `world_size ≈ CSS_px × DF / 400`. For the planned 1.04 m ultrawide screen with 1100 px CSS, DF ≈ 0.38 — but verify via Playwright screenshot at 2552×1364 before committing any DF value.
- [ ] CONSTRAINT: **R3F v9 + React 19 `frameloop="demand"` does NOT auto-paint on first scene-tree commit.** Without explicit `invalidate()` in `onCreated`, canvas stays black. FocusController GSAP camera tween also needs `invalidate()` in `onUpdate` for the dolly to actually render.
- [ ] CONSTRAINT: **CSP must allow `worker-src 'self' blob:`, `script-src ... 'wasm-unsafe-eval'`, `connect-src ... blob:`, `font-src 'self' data:`, `img-src 'self' data: blob:`** for R3F + drei + useGLTF + @fontsource. Already configured in current `index.html`; if HS redesign adds new asset types (e.g. canvas-generated texture poster) verify no new directive is needed.
- [ ] CONSTRAINT: **Real GLB workstation cannot be uniformly scaled to spec envelope.** Plan 04-06 Poly Haven assets at any uniform scale fail to satisfy spec dimensions. HS redesign Plan locks decor as procedural-only (S3=i). Do NOT swap procedural primitives for sourced GLB mid-execution; if the look demands it, pause and request a new design contract.
- [ ] CONSTRAINT: **Plan approval is gating.** No source code changes until user confirms `~/.claude/plans/neon-tabbing-workstation.md` AND the execution cadence question. Both are tracked in `human_actions_pending` of HANDOFF.json.

**Do not proceed until all five boxes are checked AND user has answered both gating questions.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting drei `distanceFactor` spec values | Spec range 1.5–2.5 is theoretical; previous session iterated 10+ times before finding empirical DF≈0.34 for 15" / ≈0.37 for 24". Plan estimates DF≈0.38 for the new 1.04 m ultrawide — must be verified. | blocking | Playwright screenshot at 2552×1364 after every DF change; compute target via `world_size = CSS_px × DF / 400` before committing. |
| Deviating from agreed scope mid-execution | Previous session wasted 5 rounds tweaking desk scale / camera position / DF direction before reading the actual UI-SPEC. Plan tasks 1–8 are the contract; do not insert "while we're here" geometry tweaks. | blocking | Read the plan top-to-bottom on resume. Each task's "Touches:" list is the file allow-list for that commit. |
| Adding real GLB for new decor | S3=i locks decor procedural. Plan 04-06 burned a session on uniform-scale GLB. | blocking | If a decor primitive looks flat, iterate emissive + lighting; do not reach for Poly Haven mid-task. v1.1 path. |
| Iterating without Playwright screenshots | Text-only iteration relies on imagined frames. Empirical Playwright loop at 2552×1364 converges 10× faster. | blocking | Every geometry/camera/DF/lighting change → vite preview at 4173 + Playwright shot before committing. |
| Cache-vs-fresh-deploy confusion | Previous session lost ~3 iterations to GH Pages CDN caching the old bundle. | advisory | After Task 8 deploy: fresh Playwright context, confirm new bundle hash in `<script type="module" src>` before declaring done. |
| Committing visual changes blind | Prior session's f256faa / 7019d08 commits were partly blind and required revert. | advisory | Always inspect screenshot diff before `git commit`. If unsure, ping user with the screenshot. |

<current_state>
**v1.0 is LIVE at https://erenatalaycs.github.io/Portfolio_Website/** at commit `6b94678` with a Phase 2 procedural 3-monitor workstation. Rollback tag `iter10-3-monitors-baseline` points here.

**This session produced no code changes.** Only artifacts:
1. Plan at `/Users/erenatalay/.claude/plans/neon-tabbing-workstation.md` — 8-task HS-redesign plan, ~1.5–2 h budget
2. Three scope decisions confirmed by user: S1 = tabs, S2 = two camera poses, S3 = all procedural decor
3. Conversation language switched to Turkish (memory entry saved at user-home; code/files stay English)

**Repo is clean** (no `git status` changes). Working state is entirely captured in HANDOFF.json + this file + the plan in user home.

**Two gating questions pending from user before code touches:**
1. Plan approval — accept the 8 tasks as written, or request edits?
2. Cadence — task-by-task with screenshot checkpoint per commit, OR batch tasks 1–3 then first checkpoint?

User answered S1/S2/S3 last but did not yet respond to the two gating questions before invoking `/gsd-pause-work`.
</current_state>

<completed_work>

- ✓ Resumed previous session, read prior HANDOFF.json + .continue-here.md
- ✓ Surfaced Track A (HS redesign) vs Track B (04-08 sign-off) split to user
- ✓ User chose Track A
- ✓ Three scope sub-questions answered: S1=i tabs / S2=ii two poses / S3=i all procedural
- ✓ Plan written to `~/.claude/plans/neon-tabbing-workstation.md` (outside repo, per prior session's path convention)
- ✓ Plan summary table presented to user with two gating questions
- ✓ Conversation switched to Turkish; feedback memory persisted
</completed_work>

<remaining_work>

- ⏳ User answer: plan approval (yes / edit) + cadence (task-by-task vs batched)
- ⏳ Task 1 — MonitorTabs.tsx + zustand tab store + BracketLink reuse
- ⏳ Task 2 — single ultrawide monitor geometry + empirical distanceFactor
- ⏳ Task 3 — two-pose camera (overview + focused) + invalidate()-safe GSAP tween
- ⏳ Task 4 — procedural desk decor (keyboard / mouse / mug / tower PC + LED)
- ⏳ Task 5 — procedural wall decor (neon strip + canvas-texture poster)
- ⏳ Task 6 — procedural chair (seat + backrest + 4 cylinder legs)
- ⏳ Task 7 — HS-style lighting tune; final visual comparison vs `/tmp/hs*.jpg`
- ⏳ Task 8 — push to main, deploy verify, fresh-Playwright CDN cache check
- ⏳ Track B (independent, 6 human-only items) — OG image, Lighthouse median-of-3, Web3Forms delivery, real-device QA, peer reviews, GitHub username decision. See `.planning/CHECKLIST-LAUNCH.md`.
</remaining_work>

<decisions_made>

- **S1 = tabs (i)** over scrollable doc / OS-windows. Cleanest, preserves click-to-focus muscle as click-to-tab, reads as terminal/IDE.
- **S2 = two camera poses (ii)** over drop-focus / per-tab poses. Click monitor → focused, click background → overview.
- **S3 = all procedural (i)** over GLB / hybrid. Locks decor as primitives; HS look achieved via emissive + lighting, not asset sourcing. v1.1 may revisit hybrid.
- **Plan filed to `~/.claude/plans/` not `.planning/`** — post-v1.0 creative iteration, not a GSD-formal phase. Lightweight plan-mode appropriate for ~1.5–2 h scope. Escalate to formal Phase 5 via `/gsd-phase` if scope grows.
- **Conversation language Turkish.** Code / commit messages / file contents / UI copy stay English (project language). Feedback memory saved.
</decisions_made>

<blockers>

- **Plan approval (human)** — gating. Cannot start Task 1 until user OKs the 8-task plan as written (or requests edits).
- **Cadence (human)** — gating. Need user choice between per-task screenshot checkpoint vs batched 1–3 then checkpoint.
- **Track B human items (parallel, non-blocking for HS redesign)** — Web3Forms account + dashboard config, OG image swap (needs deployed scene + OPSEC review), Lighthouse median-of-3, real-device QA (iOS + Android), peer reviewers ×2, GitHub username discrepancy. Tracked in `.planning/CHECKLIST-LAUNCH.md`.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured handoff (machine-readable state)
2. This file — constraints + current state
3. `/Users/erenatalay/.claude/plans/neon-tabbing-workstation.md` — the 8-task plan to be approved
4. `.planning/STATE.md` — last project state (last_activity is from before HS redesign branch)
5. `.planning/phases/02-3d-shell-asset-pipeline-capability-gating/02-UI-SPEC.md` § procedural workstation primitives — original dimensions + camera contract being intentionally superseded
6. `.planning/phases/03-content-integration-mdx-write-ups-camera-choreography/03-UI-SPEC.md` § Monitor focus poses — 3-pose contract being collapsed to 2
7. `.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-UI-SPEC.md` § Real GLB swap visual contract — superseded by S3=i procedural-only decision
8. `src/scene/*.tsx` (esp. `Workstation.tsx`, `Monitor.tsx`, `MonitorOverlay.tsx`, `cameraPoses.ts`, `FocusController.tsx`) — files Task 2 / 3 will touch
9. `src/ui/CenterMonitorContent.tsx` — current single-monitor-content surface; Task 1 refactors this

## Infrastructure State

- **Live deploy:** https://erenatalaycs.github.io/Portfolio_Website/ at `6b94678`
- **GH Pages:** source = GitHub Actions; deploy.yml runs 5 OPS-05 blocking gates + advisory Lighthouse
- **Repo Variables:** `VITE_WEB3FORMS_KEY = f2193ff4-…-012498c5a7b7` (domain-locked to erenatalaycs.github.io)
- **Self-hosted assets:** `public/draco/*` (Draco decoder, CSP-safe), `public/assets/workstation/*.glb` (Poly Haven CC0 — currently UNUSED in scene; not deleted; will remain unused while S3=i procedural-only stands)
- **Background processes:** none — no vite preview / Playwright running
- **Git tag for rollback:** `iter10-3-monitors-baseline` at `6b94678`
- **Repo state at pause:** clean — `git status` empty
- **Reference screenshots:** `/tmp/hs1.jpg` `/tmp/hs2.jpg` `/tmp/hs3.jpg` were on disk at pause time but `/tmp` may be cleared on reboot — re-download via curl from `shared.akamai.steamstatic.com/store_item_assets/steam/apps/1754840/` if missing on resume.

## Pre-Execution Critique Required

Design artifact: `/Users/erenatalay/.claude/plans/neon-tabbing-workstation.md` (the plan)

Critique focus before Task 1 starts:
- Is the 1.10 m × 0.46 m ultrawide envelope correct given desk top width 1.20 m? (Should leave ~5 cm margin both sides.)
- Does the empirical DF ≈ 0.38 estimate hold under the new screen size, or does it need re-derivation?
- Does the canvas-generated wall poster texture create a new CSP requirement? (canvas → texImage2D is same-origin, should be safe, but verify.)
- Does Task 7's cyan/teal point light interact with existing Bloom luminanceThreshold (Plan 04-01) to blow out the wall? (May need threshold bump.)

Gate: Do NOT begin Task 1 until both gating questions are answered AND the above four critique points have been considered.

<context>
This session was supposed to launch the HS redesign but landed in a planning checkpoint instead — which is the correct outcome given the scope (~1.5–2 h, breaks Phase 2/3 content contract intentionally).

The prior session's hard-won learnings (especially "always Playwright-screenshot before commit") are codified in the plan's anti-patterns and this file's constraints. The new session must not re-discover them via failure.

User chose A (major redesign) with clear sub-answers, indicating commitment to the path. The two unanswered gating questions are tiny but real — do not start coding without them, both because the agent shouldn't infer cadence preferences and because plan approval is the GSD-style gate that prevents diffs landing without explicit user OK.

Conversation language is Turkish for the user-facing chat. All files (this one, HANDOFF.json, plan, code) stay English.
</context>

<next_action>
On resume:

1. **Read this file + HANDOFF.json + the plan** at `/Users/erenatalay/.claude/plans/neon-tabbing-workstation.md`.
2. **Re-download HS reference screenshots** if `/tmp/hs*.jpg` are gone:
   ```bash
   curl -s "https://shared.akamai.steamstatic.com/store_item_assets/steam/apps/1754840/ss_808d49e0b0242082ca8a6ee6181d4eb572f169ac.600x338.jpg" -o /tmp/hs1.jpg
   curl -s "https://shared.akamai.steamstatic.com/store_item_assets/steam/apps/1754840/ss_ec52a7f8c443c5ef5023103d03c8b7bacfcccfb4.600x338.jpg" -o /tmp/hs2.jpg
   curl -s "https://shared.akamai.steamstatic.com/store_item_assets/steam/apps/1754840/ss_50349b1eb9298446ad1d57cff5a3ca8d71e6c72a.600x338.jpg" -o /tmp/hs3.jpg
   ```
3. **Surface plan summary + the two gating questions** to user (Turkish):
   - Plan approval: 8 tasks as written, or edits?
   - Cadence: per-task screenshot checkpoint vs batched 1–3?
4. **On approval, start Task 1** (MonitorTabs.tsx + zustand store) — pure refactor, no 3D change yet, lowest visual risk.
5. **Do NOT touch** anything outside the plan's "Touches:" file lists for each task. Specifically: no 2D text shell content edits, no PostFX tuning, no SEO/JSON-LD changes, no test framework changes, no GLB sourcing.
6. **Stop conditions** (from the plan): if any task exceeds 30 min iteration without converging visually → pause, screenshot, ask user. If Playwright fails to launch → fall back to manual browser screenshot. If a CSP violation appears in any task's devtools log → stop immediately, do not commit, surface to user.
7. **If anything breaks**: `git reset --hard iter10-3-monitors-baseline`. Force-push only with explicit user OK.
</next_action>
