---
phase: 04-real-asset-postprocessing-polish-pre-launch-qa
plan: 07
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
  - 04-05
  - 04-06
files_modified:
  - .planning/CHECKLIST-LAUNCH.md
  - .github/workflows/deploy.yml
  - scripts/check-parity.mjs
  - lighthouserc.json
autonomous: true
requirements:
  - OPS-03
  - OPS-05
must_haves:
  truths:
    - "`.planning/CHECKLIST-LAUNCH.md` exists with the 14-item structure from RESEARCH Pattern 18 + UI-SPEC § Pre-launch checklist files (CONTEXT D-14): Lighthouse rows, Bundle Hygiene, External Links, Security Headers, SEO+Sharing, Sitemap+Robots, CTC-01 Web3Forms verification, Reviews, Real-Device QA."
    - "`.github/workflows/deploy.yml` extended with FIVE new BLOCKING CI gates BEFORE upload-pages-artifact: dev-helper grep (no <Stats>/<Perf>/console.log/leva/r3f-perf in dist/assets/*.js); rel=noopener noreferrer audit (target=\"_blank\" without rel); CSP connect-src includes api.web3forms.com; JSON-LD email omission; npm audit --production no high/critical."
    - "`.github/workflows/deploy.yml` ALSO gains an ADVISORY non-blocking `lighthouse` job AFTER deploy with `continue-on-error: true` per CONTEXT D-17 (CI advisory; manual median-of-3 in Plan 04-08 is the OPS-03 sign-off source of truth)."
    - "`lighthouserc.json` at repo root encodes the assertion thresholds (Perf ≥0.8, A11y ≥0.9, BP ≥0.9, SEO ≥0.9; preset lighthouse:no-pwa)."
    - "`scripts/check-parity.mjs` is left unchanged (Plan 04-05 DOES NOT add the optional ContactForm-presence assertion — Vite import resolution already breaks the build if either mount is missing; PATTERNS § scripts/check-parity.mjs RECOMMENDATION: SKIP)."
    - "All five new blocking CI gates pass when the working tree is in the post-Plan-04-06 state."
  artifacts:
    - path: ".planning/CHECKLIST-LAUNCH.md"
      provides: "Pre-launch checklist with 14 explicit items + fillable rows for Lighthouse runs, peer reviews, real-device QA, Web3Forms verification"
      contains: "CHECKLIST-LAUNCH"
    - path: ".github/workflows/deploy.yml"
      provides: "5 new blocking gates + 1 advisory lighthouse job"
      contains: "Dev-helper strip"
    - path: "lighthouserc.json"
      provides: "@lhci/cli config with collect URLs, numberOfRuns=3, assert thresholds"
      contains: "categories:performance"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "lighthouserc.json"
      via: "lighthouse job runs `npx @lhci/cli@~0.15 autorun` which reads lighthouserc.json"
      pattern: "@lhci/cli"
    - from: ".planning/CHECKLIST-LAUNCH.md"
      to: ".github/workflows/deploy.yml"
      via: "checklist references the CI gates that auto-tick certain rows"
      pattern: "CI"
acceptance_grep:
  - "test -f .planning/CHECKLIST-LAUNCH.md"
  - "grep -F 'Lighthouse on deployed text shell' .planning/CHECKLIST-LAUNCH.md"
  - "grep -F 'Cyber peer review' .planning/CHECKLIST-LAUNCH.md"
  - "grep -F 'iOS device' .planning/CHECKLIST-LAUNCH.md"
  - "grep -F 'Android device' .planning/CHECKLIST-LAUNCH.md"
  - "grep -F 'CTC-01' .planning/CHECKLIST-LAUNCH.md"
  - "grep -F 'Dev-helper strip' .github/workflows/deploy.yml"
  - "grep -F 'rel=\"noopener noreferrer\"' .github/workflows/deploy.yml"
  - "grep -F 'connect-src' .github/workflows/deploy.yml"
  - "grep -F 'application/ld+json' .github/workflows/deploy.yml"
  - "grep -F 'npm audit' .github/workflows/deploy.yml"
  - "grep -F '@lhci/cli' .github/workflows/deploy.yml"
  - "grep -F 'continue-on-error: true' .github/workflows/deploy.yml"
  - "test -f lighthouserc.json"
  - "grep -F 'categories:performance' lighthouserc.json"
  - "grep -F 'lighthouse:no-pwa' lighthouserc.json"
---

<objective>
Land OPS-05 (pre-launch checklist + automatable launch CI gates) and the OPS-03 advisory CI integration:

1. Create `.planning/CHECKLIST-LAUNCH.md` per CONTEXT D-14 + RESEARCH Pattern 18 + UI-SPEC § Pre-launch checklist files. 14-item structure split: Lighthouse, Bundle Hygiene, External Links, Security Headers, SEO+Sharing, Sitemap+Robots, CTC-01 Web3Forms verification, Reviews, Real-Device QA.

2. Extend `.github/workflows/deploy.yml` with **5 NEW BLOCKING gates** BEFORE `upload-pages-artifact` (RESEARCH Pattern 19 + Pattern 20):
   - Dev-helper grep: no `<Stats>` / `<Perf>` / `console.log` / `leva` / `r3f-perf` in `dist/assets/*.js` (or all gated by `import.meta.env.DEV`).
   - External-link audit: every `target="_blank"` in `dist/index.html` carries `rel="noopener noreferrer"`.
   - CSP `connect-src` audit: `dist/index.html` includes `https://api.web3forms.com` in `connect-src` (Pitfall 3 verification).
   - JSON-LD email-omission audit: `dist/index.html` JSON-LD `<script type="application/ld+json">` does NOT contain `"email"` field (Pitfall 4 + STATE.md `[Phase 1] Plan 01-06: JSON-LD Person omits email field`).
   - `npm audit --production`: no high or critical advisories.

3. Add an ADVISORY (`continue-on-error: true`) Lighthouse-CI job after the `deploy` job per CONTEXT D-17 + RESEARCH Pattern 13/15 — runs `npx @lhci/cli autorun` against the deployed URL but does NOT block deploy.

4. Create `lighthouserc.json` at repo root encoding the assertion thresholds (Perf ≥0.8, A11y ≥0.9, BP ≥0.9, SEO ≥0.9; preset `lighthouse:no-pwa`).

5. Do NOT extend `scripts/check-parity.mjs` (PATTERNS § scripts/check-parity.mjs explicit RECOMMENDATION: SKIP — Vite import-graph already enforces ContactForm presence; an extra assertion duplicates work for low value).

Output: Automatable launch gates run on every deploy; manual rows in CHECKLIST-LAUNCH ready for Plan 04-08 sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-CONTEXT.md
@.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-RESEARCH.md
@.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-UI-SPEC.md
@.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-PATTERNS.md
@.planning/CHECKLIST-OPSEC.md
@.github/workflows/deploy.yml
@scripts/check-parity.mjs

<interfaces>
Existing `.github/workflows/deploy.yml` build job (verbatim — Phase 1 + 2 + 3 conventions):
- Steps: Checkout → Setup Node 22 → npm ci → tsc → eslint → prettier → vitest → exiftool install → strip metadata → verify metadata → npm run parity → npm run build → npx size-limit → cp dist/index.html dist/404.html → setup-pages → upload-pages-artifact.
- Then `deploy` job uses `actions/deploy-pages@v5`.

Step-naming + `run: |` block pattern (analog deploy.yml lines 60-104): `name:` + multi-line bash with explicit comments documenting the OPSEC rationale.

`lighthouserc.json` shape (RESEARCH Pattern 13 + Pattern 14):
```json
{
  "ci": {
    "collect": {
      "url": [
        "https://${HOST}/Portfolio_Website/?view=text",
        "https://${HOST}/Portfolio_Website/?view=3d"
      ],
      "numberOfRuns": 3
    },
    "upload": { "target": "temporary-public-storage" },
    "assert": {
      "preset": "lighthouse:no-pwa",
      "assertions": {
        "categories:performance": ["error", { "minScore": 0.8 }],
        "categories:accessibility": ["error", { "minScore": 0.9 }],
        "categories:best-practices": ["error", { "minScore": 0.9 }],
        "categories:seo": ["error", { "minScore": 0.9 }]
      }
    }
  }
}
```
`${HOST}` is the canonical host from Plan 04-04 Task 1.

CHECKLIST-LAUNCH.md structure (RESEARCH Pattern 18 verbatim — UI-SPEC § Pre-launch checklist files lines 290-303 are authoritative). Sections:
- Lighthouse on Deployed URL (OPS-03 + CONTEXT D-17) — text shell + 3D shell rows with Run 1/2/3 + median fillable
- Bundle Hygiene — dev-helper strip + npm audit + npm ls
- External Links — `rel="noopener noreferrer"` audit
- Security Headers — CSP `<meta>` + `connect-src` audit
- SEO + Sharing — OG image dimensions + LinkedIn/Slack preview + JSON-LD validator + title + description
- Sitemap + Robots — file present + URL count
- CTC-01 Web3Forms Verification — Gmail + Outlook delivery rows
- Reviews — cyber peer + non-cyber usability (CONTEXT D-15)
- Real-Device QA — iOS + Android (CONTEXT D-16)
</interfaces>

<key_decisions>
- **Scope split (CONTEXT D-14):** keep `.planning/CHECKLIST-OPSEC.md` OPSEC-only (existing); add `.planning/CHECKLIST-LAUNCH.md` for operational launch concerns. Phase 4 verification reads BOTH.
- **5 new BLOCKING gates in deploy.yml (RESEARCH Pattern 19 recommendation):** dev-helper, target=_blank rel audit, CSP connect-src, JSON-LD email omission, npm audit. All exit-fail → block deploy.
- **Lighthouse CI advisory only (CONTEXT D-17):** the manual deployed-URL run (Plan 04-08 median-of-3) remains the OPS-03 sign-off source of truth. CI is for build-trend artifact + early warning. `continue-on-error: true` on the lighthouse job ensures advisory.
- **`@lhci/cli` via `npx`, NOT installed (RESEARCH Standard Stack)** — keeps package.json clean for one-shot tool. Verify version pin: `npx @lhci/cli@~0.15.1`.
- **`lighthouserc.json` committed to repo root (RESEARCH Pattern 13)** — clearer than CLI flags; reusable from manual run + CI run.
- **Lighthouse runs against the canonical host from Plan 04-04** — `lighthouserc.json` URL strings reference the canonical host (Plan 04-04 Task 1 decision). Eren's manual median-of-3 in Plan 04-08 uses the same URLs.
- **Wait + warmup before Lighthouse run (RESEARCH Pitfall 8):** the lighthouse CI job waits ~60s after deploy then curls the deployed URL once to warm the CDN before running lhci. Documented in deploy.yml step.
- **DO NOT extend `scripts/check-parity.mjs` (PATTERNS § scripts/check-parity.mjs RECOMMENDATION: SKIP)** — Vite's import graph already breaks the build if `<ContactForm />` is missing from `<Contact />`. The extra assertion duplicates work; the parity script already covers TXT-06 + CNT-03 + Pitfall 8.
- **Plaintext-email-in-JSON-LD audit (RESEARCH Pattern 20):** the gate uses the `sed | grep '"email"'` form (not the more aggressive "grep for actual email TLD" form which requires CI to know the plaintext). The omission gate is sufficient; the actual-TLD gate is OPSEC-checklist-territory (Eren can run it manually).
- **`npm audit --production` (RESEARCH Pattern 19):** runs against production deps only; dev deps not part of shipped bundle. Fail on high/critical only (info/low warnings allowed).
</key_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .planning/CHECKLIST-LAUNCH.md</name>
  <files>.planning/CHECKLIST-LAUNCH.md</files>
  <behavior>
    - File at `.planning/CHECKLIST-LAUNCH.md` with header + intro + 9 sections per RESEARCH Pattern 18 + UI-SPEC § Pre-launch checklist files (CONTEXT D-14).
    - Items use `- [ ]` markdown checkboxes. Some items prefixed `**[CI]**` (CHECKLIST-OPSEC convention) when the CI gate auto-ticks them; others are author-owned manual.
    - Fillable rows use `___` placeholders (e.g. `Reviewer: ___, Date: ___`) per UI-SPEC.
    - Footer documents last-updated date + run-before-release rule.
  </behavior>
  <action>
    Create `.planning/CHECKLIST-LAUNCH.md` per RESEARCH Pattern 18 verbatim structure (UI-SPEC § Pre-launch checklist files lines 290-303 + Pattern 18 line 1168-1257 — combine):

    ```markdown
    # Pre-Launch Checklist

    > Owned by Phase 4 (Plan 04-07 created; Plan 04-08 fills). Run before
    > tagging v1.0 release. Distinct from CHECKLIST-OPSEC.md which stays
    > OPSEC-only. Phase 4 verification reads BOTH; both must be fully
    > checked before milestone-complete.

    ## Lighthouse on Deployed URL (OPS-03 + CONTEXT D-17)

    Manual median-of-3 is the OPS-03 sign-off source of truth (CONTEXT D-17).
    The CI job in `.github/workflows/deploy.yml` is advisory only (`continue-on-error: true`).

    Warmup protocol (RESEARCH Pitfall 8): curl the deployed URLs once to
    warm CDN cache, sleep 5s, then run lhci 3x.

    - [ ] Lighthouse on deployed text shell — Performance ≥80, Accessibility ≥90, Best Practices ≥90, SEO ≥90 — runs documented (3 runs, median):
      - Run 1: Perf=__, A11y=__, BP=__, SEO=__
      - Run 2: Perf=__, A11y=__, BP=__, SEO=__
      - Run 3: Perf=__, A11y=__, BP=__, SEO=__
      - Median: Perf=__, A11y=__, BP=__, SEO=__
      - Verdict: PASS / FAIL
    - [ ] Lighthouse on deployed 3D shell (advisory) — no error / no blank page; numbers documented:
      - Run 1: Perf=__, A11y=__, BP=__, SEO=__
      - Median: Perf=__, A11y=__, BP=__, SEO=__

    ## Bundle Hygiene

    - [ ] **[CI]** Dev-helper strip — `dist/assets/*.js` does not contain `<Stats>`, `<Perf>`, `console.log`, `leva`, `r3f-perf` (or all gated by `import.meta.env.DEV`).
    - [ ] **[CI]** `npm audit --production` returns no high or critical advisories.
    - [ ] `npm ls` shows no unexpected dependencies (e.g. transitive deps that pull analytics).

    ## External Links

    - [ ] **[CI]** Every `target="_blank"` in `dist/index.html` carries `rel="noopener noreferrer"`.
    - [ ] LinkedIn URL is the public profile URL, not a logged-in-state URL with tracking params.
    - [ ] GitHub URL is the user profile, not a starred-from / search URL.

    ## Security Headers

    - [ ] **[CI]** CSP `<meta>` tag present and includes `connect-src 'self' https://api.web3forms.com` (Pitfall 3 verification).
    - [ ] **[CI]** CSP `<meta>` tag present and includes `form-action 'self' https://api.web3forms.com`.
    - [ ] Sourcemaps disabled in `vite.config.ts` (`build.sourcemap: false`).

    ## SEO + Sharing

    - [ ] OG image (1200×630) verified — opens in browser at deployed URL, dimensions correct, no GH-Pages-base-path 404:
      ```bash
      curl -I https://{HOST}/Portfolio_Website/assets/og-image.png
      # Expect 200 OK, content-type: image/png
      ```
    - [ ] OG card preview verified by pasting deployed URL into LinkedIn DM compose AND Slack DM compose (manual visual check).
    - [ ] OG image full-resolution OPSEC review (RESEARCH Pitfall 4): no notification banners (Slack/Discord/Mail), no extension badges, no autofill, no localhost in URL bar, no employer-NDA content, no partial face/room reflection.
    - [ ] **[CI]** JSON-LD Person schema present in `dist/index.html` AND does NOT contain `"email"` field (Phase 1 OPSEC carry-over per Pitfall 4).
    - [ ] JSON-LD Person schema validates on https://validator.schema.org/.
    - [ ] `<title>` is `Eren Atalay — Junior Cybersecurity Analyst (UK)` (UI-SPEC verbatim).
    - [ ] `<meta name="description">` is `Junior cybersecurity analyst from the UK — CV, projects, CTF write-ups, and a 3D hacker workstation portfolio.` (UI-SPEC verbatim, ≤155 chars).

    ## Sitemap + Robots

    - [ ] **[CI]** `dist/sitemap.xml` present, lists exactly 6 URLs (/ + ?view=3d + 4 ?focus=...), each with `<loc>` and `<lastmod>` only (no `<changefreq>` / `<priority>`).
    - [ ] **[CI]** `dist/robots.txt` present, includes `Sitemap:` line pointing to deployed sitemap.xml.

    ## CTC-01 Web3Forms Verification (RESEARCH Pattern 10)

    - [ ] Web3Forms account created; access key set in repo Variables (Settings → Secrets and variables → Actions → Variables → `VITE_WEB3FORMS_KEY`).
    - [ ] Web3Forms notification email set to `eren.atalay@…` in dashboard.
    - [ ] Real submission tested end-to-end → arrived in **Gmail** inbox, NOT spam:
      - Date/time: __
      - Inbox / Spam: __
      - Reply-to set correctly (Reply target = submitter's email): __
    - [ ] Real submission tested end-to-end → arrived in **Outlook/Hotmail** inbox, NOT spam:
      - Date/time: __
      - Inbox / Spam: __
      - Reply-to set correctly: __
    - [ ] Subject line verified: arrives as `[Portfolio enquiry] from <Submitter Name>`.
    - [ ] Honeypot manual test — fill `botcheck` via DevTools, click submit, confirm: success UI shows BUT no submission arrives in inbox.

    ## Reviews (CONTEXT D-15)

    - [ ] Cyber peer review — Reviewer: ___, Date: ___, Verdict: ___, Notes: ___
    - [ ] Non-cyber usability review — Reviewer: ___, Date: ___, Verdict: ___, Notes: ___

    ## Real-Device QA (OPS-04 + CONTEXT D-16) — RESEARCH Pattern 16 protocol

    Per-device protocol: cold-load text shell on cellular (<5s); reveal email; submit a real test message; open `?view=3d` shell; scroll inside 3D shell; toggle prefers-reduced-motion ON + reload; 30-min soak with 3D shell open in foreground.

    - [ ] iOS device — Model: ___, iOS version: ___, Test date: ___, Text shell: PASS/FAIL, 3D shell: PASS/FAIL/REFUSED-GRACEFULLY, Notes: ___
    - [ ] Android device — Model: ___, Android version: ___, Test date: ___, Text shell: PASS/FAIL, 3D shell: PASS/FAIL/REFUSED-GRACEFULLY, Notes: ___
    - [ ] (Optional) iPad — Model: ___, iPadOS version: ___, Test date: ___, Text shell: PASS/FAIL, 3D shell: PASS/FAIL/REFUSED-GRACEFULLY, Notes: ___

    ## 3D-08 GLB Status (Plan 04-06)

    - [ ] Path A — Real GLB shipped within CONTEXT D-04 7-day timebox. Total `public/assets/workstation/*.glb` size: ___ KB. CC0 sources verified in `LICENSE.txt`. Visual emissive bloom confirmed in dev. Real-device readability confirmed in iOS + Android rows above.

    OR

    - [ ] Path B — Timebox expired; GLB scope DEFERRED to v1.1. 3D-08 marked PARTIAL (postprocessing in Plan 04-01 ships, GLB doesn't). V2-3D-01 promoted to first-class in v1.1 milestone.

    ---

    > Last updated: 2026-MM-DD · run before v1.0 release.
    ```

    Replace `{HOST}` with the canonical host from Plan 04-04. Save the file.
  </action>
  <verify>
    <automated>test -f .planning/CHECKLIST-LAUNCH.md &amp;&amp; grep -F 'Lighthouse on deployed text shell' .planning/CHECKLIST-LAUNCH.md &amp;&amp; grep -F 'CTC-01' .planning/CHECKLIST-LAUNCH.md &amp;&amp; grep -F 'Cyber peer review' .planning/CHECKLIST-LAUNCH.md &amp;&amp; grep -F 'iOS device' .planning/CHECKLIST-LAUNCH.md &amp;&amp; grep -F 'Android device' .planning/CHECKLIST-LAUNCH.md &amp;&amp; grep -F 'Dev-helper strip' .planning/CHECKLIST-LAUNCH.md</automated>
  </verify>
  <done>
    `CHECKLIST-LAUNCH.md` exists with all 9 sections + 14+ items + fillable rows. Items prefixed `**[CI]**` correspond to gates added in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create lighthouserc.json + extend deploy.yml with 5 new blocking gates + advisory lighthouse job</name>
  <files>lighthouserc.json, .github/workflows/deploy.yml</files>
  <behavior>
    - `lighthouserc.json` at repo root encodes the assertion thresholds (Perf ≥0.8, A11y ≥0.9, BP ≥0.9, SEO ≥0.9) + collect URLs (text-shell + 3D-shell on the canonical host) + numberOfRuns=3 + upload temporary-public-storage + preset lighthouse:no-pwa.
    - `.github/workflows/deploy.yml` build job gains 5 new BLOCKING steps inserted AFTER `Build (Vite)` + AFTER `Enforce bundle size budgets` + BEFORE `Copy index.html → 404.html`:
      1. **Dev-helper strip** — grep `dist/assets/*.js` for `<Stats|<Perf|console\.log|leva|r3f-perf`; fail if any match (Pattern 19 + Pattern 20).
      2. **External-link audit** — grep `dist/index.html` for `target="_blank"` lines; check each carries `rel="noopener noreferrer"`. Fail if any unmatched.
      3. **CSP connect-src audit** — grep `dist/index.html` for `connect-src.*https://api.web3forms.com`. Fail if absent (Pitfall 3 verification).
      4. **JSON-LD email omission** — extract the JSON-LD block from `dist/index.html` via `sed`; grep for `"email"`. Fail if found (Pattern 20 verification).
      5. **`npm audit --production`** — fail on high/critical advisories.
    - `.github/workflows/deploy.yml` gains a NEW JOB `lighthouse` AFTER the `deploy` job. `needs: deploy`. `continue-on-error: true`. Steps: setup-node + sleep 60 (CDN warmup) + curl -s -o /dev/null warmup (RESEARCH Pitfall 8) + `npx @lhci/cli@~0.15 autorun`.
    - All gate command output should be visible in the GH Actions log for diagnostic purposes.
  </behavior>
  <action>
    Step 1 — Create `lighthouserc.json` at repo root per RESEARCH Pattern 13. Substitute `{HOST}` with the canonical host from Plan 04-04:
    ```json
    {
      "ci": {
        "collect": {
          "url": [
            "https://{HOST}/Portfolio_Website/?view=text",
            "https://{HOST}/Portfolio_Website/?view=3d"
          ],
          "numberOfRuns": 3
        },
        "upload": {
          "target": "temporary-public-storage"
        },
        "assert": {
          "preset": "lighthouse:no-pwa",
          "assertions": {
            "categories:performance": ["error", { "minScore": 0.8 }],
            "categories:accessibility": ["error", { "minScore": 0.9 }],
            "categories:best-practices": ["error", { "minScore": 0.9 }],
            "categories:seo": ["error", { "minScore": 0.9 }]
          }
        }
      }
    }
    ```

    Step 2 — Edit `.github/workflows/deploy.yml`. After the existing `Enforce bundle size budgets` step (line 122-123) and BEFORE the `Copy index.html → 404.html` step (line 125-126), insert FIVE new steps:

    ```yaml
      # Phase 4 OPS-05 — operational launch gates (Plan 04-07).

      - name: Dev-helper strip (no <Stats>, <Perf>, console.log, leva, r3f-perf in dist)
        run: |
          # Source: 04-RESEARCH.md Pattern 19 + Pattern 20.
          # Fail if any dev helper leaks into the production bundle UNLESS
          # explicitly tagged `// dev-only` (manual exception marker).
          # console.warn / console.error are KEPT (acceptable error reporting).
          if grep -rE "(<Stats|<Perf|console\.log|console\.debug|leva|r3f-perf)" dist/assets/ 2>/dev/null \
              | grep -v "// dev-only" \
              | grep -v "^Binary file"; then
            echo "OPS-05 FAIL: dev helper(s) leaked into dist/. Gate behind import.meta.env.DEV."
            exit 1
          fi
          echo "Dev-helper strip: OK."

      - name: External-link audit (every target=\"_blank\" carries rel=\"noopener noreferrer\")
        run: |
          # Source: 04-RESEARCH.md Pattern 19. CHECKLIST-OPSEC § External Links.
          # OPS-05 explicit. Phase 1 BracketLink already auto-attaches rel for
          # external links; this gate verifies no hand-rolled <a target=\"_blank\">
          # slipped past.
          offenders=$(grep -E 'target="_blank"' dist/index.html | grep -v 'rel="noopener noreferrer"' || true)
          if [ -n "$offenders" ]; then
            echo "OPS-05 FAIL: target=\"_blank\" without rel=\"noopener noreferrer\":"
            echo "$offenders"
            exit 1
          fi
          # Also audit the bundled JS — any inlined-string anchor.
          if grep -rE 'target="_blank"' dist/assets/ 2>/dev/null | grep -v 'rel=\\?"noopener noreferrer\\?"' | grep -v '^Binary file'; then
            echo "OPS-05 FAIL: target=\"_blank\" in dist/assets/ without rel."
            exit 1
          fi
          echo "External-link audit: OK."

      - name: CSP connect-src includes api.web3forms.com (Pitfall 3)
        run: |
          # Source: 04-RESEARCH.md Pitfall 3. Plan 04-02 extended the CSP;
          # this gate verifies the extension survives into dist/.
          if ! grep -F 'connect-src' dist/index.html | grep -F 'https://api.web3forms.com' >/dev/null; then
            echo "OPS-05 FAIL: dist/index.html CSP <meta> does not allow Web3Forms in connect-src."
            echo "ContactForm submit will be blocked silently in production."
            exit 1
          fi
          echo "CSP connect-src: OK."

      - name: JSON-LD email-omission audit (Pitfall 4)
        run: |
          # Source: 04-RESEARCH.md Pattern 20. Phase 1 D-09 + STATE.md
          # `[Phase 1] Plan 01-06: JSON-LD Person omits email field`.
          # This gate verifies the omission survives the build. Plain-email
          # leak via JSON-LD defeats the rot13+base64 obfuscation.
          if ! grep -F 'application/ld+json' dist/index.html >/dev/null; then
            echo "OPS-05 FAIL: dist/index.html missing JSON-LD <script> entirely."
            exit 1
          fi
          if sed -n '/application\/ld+json/,/<\/script>/p' dist/index.html | grep -E '"email"\s*:' >/dev/null; then
            echo "OPS-05 FAIL: JSON-LD includes \"email\" field (Pitfall 4 information disclosure)."
            exit 1
          fi
          echo "JSON-LD email-omission: OK."

      - name: npm audit --production (no high/critical)
        run: |
          # Source: 04-RESEARCH.md Pattern 19. Production deps only;
          # dev deps not in shipped bundle. Fails on high/critical.
          npm audit --production --audit-level=high
    ```

    Step 3 — Append a NEW JOB `lighthouse` to `deploy.yml` AFTER the existing `deploy` job (line 145):

    ```yaml
      lighthouse:
        # Source: 04-CONTEXT.md D-17; 04-RESEARCH.md Pattern 13 + Pattern 15 +
        #   Pitfall 8. ADVISORY only — manual median-of-3 in Plan 04-08 is the
        #   OPS-03 sign-off source of truth. continue-on-error: true keeps
        #   deploy unblocked even if scores drop.
        needs: deploy
        runs-on: ubuntu-latest
        if: ${{ success() }}
        continue-on-error: true
        steps:
          - name: Checkout (for lighthouserc.json)
            uses: actions/checkout@v4

          - name: Setup Node
            uses: actions/setup-node@v6
            with:
              node-version: '22'
              cache: 'npm'

          - name: Wait for GH Pages CDN propagation + warm cache (Pitfall 8)
            run: |
              sleep 60
              # Warm CDN cache to make first lighthouse run representative.
              curl -s -o /dev/null https://{HOST}/Portfolio_Website/ || true
              curl -s -o /dev/null "https://{HOST}/Portfolio_Website/?view=text" || true
              curl -s -o /dev/null "https://{HOST}/Portfolio_Website/?view=3d" || true
              sleep 5

          - name: Run Lighthouse-CI (advisory)
            run: npx -y @lhci/cli@~0.15.1 autorun
    ```

    Substitute `{HOST}` with the canonical host. Update step naming + `run: |` block per existing deploy.yml convention (multi-line bash + comments documenting rationale).

    Step 4 — Commit and push. Verify the workflow YAML is valid: `npx yaml-lint .github/workflows/deploy.yml` (if yaml-lint isn't available, use `node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/deploy.yml', 'utf8'))"`).

    Step 5 — Trigger a build via push (or `workflow_dispatch`). Verify all five new gates run + pass. Verify the `lighthouse` job runs after deploy + produces results (or `continue-on-error: true` lets deploy succeed even on gate failure).

    Step 6 — Run `npm run typecheck && npm test --run && npm run lint && npm run format && npm run parity && npm run build && npm run size && npm audit --production --audit-level=high` LOCALLY to verify each gate matches local equivalent.
  </action>
  <verify>
    <automated>test -f lighthouserc.json &amp;&amp; grep -F 'categories:performance' lighthouserc.json &amp;&amp; grep -F 'lighthouse:no-pwa' lighthouserc.json &amp;&amp; grep -F 'Dev-helper strip' .github/workflows/deploy.yml &amp;&amp; grep -F 'rel="noopener noreferrer"' .github/workflows/deploy.yml &amp;&amp; grep -F 'connect-src' .github/workflows/deploy.yml &amp;&amp; grep -F 'application/ld+json' .github/workflows/deploy.yml &amp;&amp; grep -F 'npm audit' .github/workflows/deploy.yml &amp;&amp; grep -F '@lhci/cli' .github/workflows/deploy.yml &amp;&amp; grep -F 'continue-on-error: true' .github/workflows/deploy.yml &amp;&amp; node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/deploy.yml', 'utf8'))" &amp;&amp; npm run typecheck &amp;&amp; npm test --run &amp;&amp; npm run build &amp;&amp; npm audit --production --audit-level=high</automated>
  </verify>
  <done>
    `lighthouserc.json` valid + thresholds set. `deploy.yml` has 5 new blocking gates + 1 advisory lighthouse job. Local equivalents of each gate pass against current dist/. Workflow YAML parses cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GH Actions runner ↔ npm registry | `npx @lhci/cli` downloads on demand; trust npm registry signing |
| Build pipeline ↔ deployed artifact | New gates verify `dist/` before upload-pages-artifact — last line of defense before recruiter visibility |
| Author repo ↔ checklist file | Manual rows are author-owned; cannot be CI-enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Information Disclosure | Plaintext email leak via JSON-LD survives to production | mitigate | New JSON-LD email-omission gate in deploy.yml; CHECKLIST-LAUNCH manual row re-verifies |
| T-04-07-02 | Tampering | npm transitive CVE in shipped dep | mitigate | New `npm audit --production --audit-level=high` gate; fails on high/critical |
| T-04-07-03 | Misconfiguration | CSP regression strips `https://api.web3forms.com` from `connect-src` | mitigate | New CSP-audit gate verifies the extension survives every build |
| T-04-07-04 | Information Disclosure | Dev helper (`<Stats>`, `console.log`, `leva`) leaks into prod bundle | mitigate | New dev-helper-strip gate; allows `// dev-only` comment marker exception |
| T-04-07-05 | Tampering | External link reverse-tabnabbing if `rel="noopener noreferrer"` missing | mitigate | New external-link-audit gate; greps both `dist/index.html` and `dist/assets/*.js` |
| T-04-07-06 | Tampering | Lighthouse-CI uploads to temporary-public-storage — exposes scores publicly | accept | Scores are not sensitive; OPS-03 explicitly publishes thresholds; recruiter can run their own Lighthouse audit anyway. CONTEXT D-17 explicit. |
| T-04-07-07 | Denial of Service | npm audit blocks deploy on a transitive low-priority CVE that has no patch | accept | `--audit-level=high` only fails on high/critical, not moderate/low. If a high CVE has no patch, defer/exception via deploy.yml comment + temporary downgrade — escalate as deviation. |
</threat_model>

<verification>
**Per-task verification:** see each task's `<verify>` block.

**Plan-level verification:**
1. `lighthouserc.json` parses as JSON; encodes the four assertion thresholds.
2. `deploy.yml` parses as YAML; new steps run before upload; advisory lighthouse job runs after deploy.
3. All 5 new gates pass locally against current `dist/` (post-Plan-04-06 state).
4. CHECKLIST-LAUNCH.md exists with all 9 sections; rows ready for Plan 04-08 sign-off.
5. `scripts/check-parity.mjs` is UNCHANGED (PATTERNS recommendation).
</verification>

<success_criteria>
- `.planning/CHECKLIST-LAUNCH.md` exists per CONTEXT D-14 + RESEARCH Pattern 18 verbatim structure.
- `lighthouserc.json` exists with assertion thresholds + collect URLs.
- `.github/workflows/deploy.yml` has 5 new blocking gates (dev-helper, target=_blank rel, CSP connect-src, JSON-LD email omission, npm audit) BEFORE upload-pages-artifact.
- `.github/workflows/deploy.yml` has 1 new advisory `lighthouse` job AFTER deploy with `continue-on-error: true`.
- All gates pass locally on current dist/.
- `scripts/check-parity.mjs` unchanged (PATTERNS recommendation).
- Pitfall 3 enforced. Pitfall 4 enforced. Pitfall 8 mitigated (warmup before lhci).
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-07-SUMMARY.md` documenting:
- Each new gate's first-run result (pass/fail) on the local dist/.
- The advisory Lighthouse job's first-deploy results (Performance / Accessibility / BestPractices / SEO scores) — read from GH Actions log; if scores fail thresholds, this is INFORMATION ONLY (advisory). Plan 04-08 manual median-of-3 is the OPS-03 sign-off.
- Confirmation that `scripts/check-parity.mjs` is unchanged (PATTERNS recommendation honored).
</output>
