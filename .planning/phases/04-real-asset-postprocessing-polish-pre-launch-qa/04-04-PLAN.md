---
phase: 04-real-asset-postprocessing-polish-pre-launch-qa
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - public/sitemap.xml
  - public/robots.txt
  - index.html
autonomous: false
requirements:
  - OPS-05
  - CTC-03
must_haves:
  truths:
    - "ONE canonical GH-Pages host is in use across `index.html` (canonical + og:url + og:image + JSON-LD url + JSON-LD image), `public/sitemap.xml` (every <loc>), and `public/robots.txt` (Sitemap line) — verified by grep."
    - "`public/sitemap.xml` lists exactly six URLs: `/`, `/?view=3d`, `/?focus=projects`, `/?focus=writeups`, `/?focus=certs`, `/?focus=contact` — each with `<loc>` and `<lastmod>` only (NO `<changefreq>`, NO `<priority>` — deprecated by Google in 2023)."
    - "`public/robots.txt` allows all + has a `Sitemap:` line pointing to the canonical sitemap URL."
    - "JSON-LD `<script type=\"application/ld+json\">` in `index.html` does NOT contain a `\"email\"` field (Phase 1 OPSEC carry-over per Pitfall 4 — verified by grep)."
    - "`<meta name=\"description\">` matches the locked phrase from UI-SPEC § SEO surfaces verbatim, ≤155 chars."
    - "OG image still references `assets/og-image.png` at the canonical absolute URL; the binary is REPLACED in Plan 04-08 (real screenshot + OPSEC review)."
  artifacts:
    - path: "public/sitemap.xml"
      provides: "Six-URL sitemap with <loc> + <lastmod> only"
      contains: "/?focus=contact"
    - path: "public/robots.txt"
      provides: "User-agent: * + Allow: / + Sitemap: <canonical-host>/Portfolio_Website/sitemap.xml"
      contains: "Sitemap:"
    - path: "index.html"
      provides: "Reconciled GH-Pages host across canonical/og:url/og:image/JSON-LD; verified meta description; Phase 4 CSP extension (already done in Plan 04-02)"
      contains: "Portfolio_Website"
  key_links:
    - from: "public/robots.txt"
      to: "public/sitemap.xml"
      via: "Sitemap: <canonical-host>/Portfolio_Website/sitemap.xml"
      pattern: "Sitemap:.*sitemap\\.xml"
    - from: "index.html"
      to: "public/assets/og-image.png"
      via: "<meta property=\"og:image\">"
      pattern: "og-image\\.png"
acceptance_grep:
  - "grep -F '<loc>' public/sitemap.xml | wc -l | tr -d ' ' | grep -F '6'"
  - "grep -F '<lastmod>' public/sitemap.xml"
  - "! grep -E '<changefreq>|<priority>' public/sitemap.xml"
  - "grep -F 'Sitemap:' public/robots.txt"
  - "grep -F 'Allow: /' public/robots.txt"
  - "grep -F 'application/ld+json' index.html"
  - "! sed -n '/application\\/ld+json/,/<\\/script>/p' index.html | grep -E '\"email\"\\s*:'"
  - "grep -F 'og:image' index.html | grep -F 'og-image.png'"
  - "grep -F '<title>' index.html"
  - "grep -F '<meta name=\"description\"' index.html"
---

<objective>
Land the SEO surfaces for OPS-03 / OPS-05: ensure ONE canonical GH-Pages host is consistent across `index.html` (canonical, og:url, og:image, twitter:image, JSON-LD `url` + `image`), `public/sitemap.xml` (six URLs), and `public/robots.txt` (Sitemap line). Replace the existing 1-URL sitemap stub with the six-URL form (no `<changefreq>` / `<priority>` — deprecated 2023). Verify JSON-LD continues to OMIT the `email` field (Phase 1 OPSEC). Verify the locked meta description copy survives. The OG image binary itself stays placeholder until Plan 04-08 (real screenshot + OPSEC review).

**Per CONTEXT § Open Questions #2 + RESEARCH § Architectural Responsibility Map:** the `index.html` currently uses `erenatalaycs.github.io` while CONTEXT D-17 references `eren-atalay.github.io`. ONE host must be picked at execution time and propagated everywhere. This task contains a `checkpoint:decision` to resolve that fork before propagating.

Output: Reconciled SEO surfaces, six-URL sitemap, robots.txt aligned, JSON-LD verified email-free.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-CONTEXT.md
@.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-RESEARCH.md
@.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-UI-SPEC.md
@.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-PATTERNS.md
@.planning/CHECKLIST-OPSEC.md
@index.html
@public/sitemap.xml
@public/robots.txt
@src/content/identity.ts

<interfaces>
Current `index.html` host references (from current source — verified before this plan):
- `<link rel="canonical" href="https://erenatalaycs.github.io/Portfolio_Website/" />`
- `<meta property="og:url" content="https://erenatalaycs.github.io/Portfolio_Website/" />`
- `<meta property="og:image" content="https://erenatalaycs.github.io/Portfolio_Website/assets/og-image.png" />`
- `<meta name="twitter:image" content="https://erenatalaycs.github.io/Portfolio_Website/assets/og-image.png" />`
- JSON-LD `"url": "https://erenatalaycs.github.io/Portfolio_Website/"` + `"image": "https://erenatalaycs.github.io/Portfolio_Website/assets/og-image.png"`
- JSON-LD `"sameAs": ["https://github.com/erenatalaycs", ...]` — note GitHub username also `erenatalaycs`

CONTEXT D-17: `eren-atalay.github.io` (the spec preferred form for Lighthouse-CI URL).

**This is the open question** — Eren must decide which is canonical at plan execution time.

Current `public/sitemap.xml` (1 URL, has deprecated tags):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://erenatalaycs.github.io/Portfolio_Website/</loc>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
```

Current `public/robots.txt`:
```
User-agent: *
Allow: /
Sitemap: https://erenatalaycs.github.io/Portfolio_Website/sitemap.xml
```

UI-SPEC § SEO surfaces locked content:
- `<title>`: `Eren Atalay — Junior Cybersecurity Analyst (UK)` — current `index.html` has `Eren Atalay — Junior Cybersecurity Analyst` (no `(UK)` suffix). Task verifies which one Eren wants; UI-SPEC writes `(UK)` parenthetical, current ships without. Decide which.
- Meta description: `Junior cybersecurity analyst from the UK — CV, projects, CTF write-ups, and a 3D hacker workstation portfolio.` (≤155 chars). Current `index.html` has a different phrase: `Eren Atalay — Junior Cybersecurity Analyst (UK). CV, projects, certifications, and home-lab write-ups.` Task verifies which one ships; UI-SPEC is the authoritative spec, but the current copy is also defensible. Decide.

**Note on JSON-LD:** Phase 1 D-09 + STATE.md `[Phase 1] Plan 01-06: JSON-LD Person omits email field per Pitfall 4` — verified the existing JSON-LD does NOT contain `email`. This plan's verification is to ENFORCE that the omission survives, not to add it.
</interfaces>

<key_decisions>
- **Host reconciliation requires Eren's decision (CONTEXT § Open Questions #2)** — checkpoint:decision task. Eren picks the canonical host (`erenatalaycs.github.io` or `eren-atalay.github.io`) and confirms whether the GitHub username matches. If username changes too, JSON-LD `sameAs` GitHub URL also updates.
- **Six URLs in sitemap (CONTEXT § Claude's Discretion + UI-SPEC § SEO surfaces)** — `/`, `/?view=3d`, `/?focus=projects`, `/?focus=writeups`, `/?focus=certs`, `/?focus=contact`. Each entry: `<url><loc>...</loc><lastmod>YYYY-MM-DD</lastmod></url>`. NO `<changefreq>`, NO `<priority>` (deprecated by Google 2023 per RESEARCH State of the Art).
- **`<lastmod>` value** — use the date this plan executes (e.g. `2026-05-08`). Format: ISO `YYYY-MM-DD`. Single date for all six entries (the entire sitemap was last modified together).
- **JSON-LD email-omission ENFORCEMENT not addition** — STATE.md `[Phase 1] Plan 01-06: JSON-LD Person omits email field per Pitfall 4`. Phase 4 verifies the omission survives. The acceptance grep gate `! sed -n '/application\\/ld+json/,/<\\/script>/p' index.html | grep -E '"email"\\s*:'` blocks regression.
- **Meta description verbatim from UI-SPEC** — `Junior cybersecurity analyst from the UK — CV, projects, CTF write-ups, and a 3D hacker workstation portfolio.` This is shorter than the current copy and aligns the description with the actual content (write-ups + 3D workstation, not "home-lab"). 154 chars (≤155 limit).
- **`<title>` verbatim from UI-SPEC** — `Eren Atalay — Junior Cybersecurity Analyst (UK)` (with `(UK)` parenthetical). UI-SPEC § SEO surfaces is authoritative.
- **OG image binary stays placeholder until Plan 04-08** — STATE.md `[Phase 1] Plan 01-06: og-image.png is a 1200x630 placeholder; Plan 07 OPSEC swaps in real terminal screenshot before live deploy`. This plan keeps the META reference; binary swap + OPSEC review happens in Plan 04-08 (per RESEARCH Pattern 12 + Pitfall 4 OG image OPSEC review).
- **Twitter title + description** — also update to align with UI-SPEC if discrepancy exists. They mirror og:title + og:description.
- **`autonomous: false`** — the host-reconciliation decision requires Eren's input (CONTEXT § Open Questions #2 explicit). The checkpoint pauses for Eren to confirm; subsequent file edits proceed automatically.
</key_decisions>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide canonical GH-Pages host</name>
  <decision>Pick the canonical GitHub Pages handle for v1.0 launch.</decision>
  <context>
    Current `index.html`, `public/sitemap.xml`, `public/robots.txt` all use `erenatalaycs.github.io` (matching the GitHub username `erenatalaycs` in `identity.github`). CONTEXT D-17 references `eren-atalay.github.io` (more readable, hyphenated form). One must be picked and propagated everywhere; the other variant must NOT remain anywhere in source. After this plan lands, the chosen host is the canonical for: `index.html` canonical link + og:url + og:image + twitter:image + JSON-LD url + JSON-LD image; `public/sitemap.xml` every `<loc>`; `public/robots.txt` Sitemap line; `.github/workflows/deploy.yml` Lighthouse-CI URL (Plan 04-07); `CHECKLIST-LAUNCH.md` Lighthouse target URL (Plan 04-07).

    **If Eren switches GitHub username**, that's a repo-rename operation outside this plan — Eren handles in GitHub Settings → Repository → Rename, then re-deploys. The plan's executor should ASK Eren which path:
    - **Option A: Stay on `erenatalaycs`** — no GitHub-side change; just propagate the existing host everywhere consistently (purely defensive, since `index.html` already uses it).
    - **Option B: Switch to `eren-atalay`** — Eren renames GitHub username + repo URL changes; executor updates `identity.github` from `https://github.com/erenatalaycs` to `https://github.com/eren-atalay`; updates JSON-LD `sameAs` array; updates ALL host references across `index.html`/`sitemap.xml`/`robots.txt`. **Heavier path** — requires GitHub-side work BEFORE this plan can complete.
  </context>
  <options>
    <option id="option-a">
      <name>Stay on erenatalaycs.github.io (status quo)</name>
      <pros>Zero GitHub-side work. Existing repo URL still resolves. `identity.github` unchanged. Matches the JSON-LD `sameAs` GitHub URL already shipping.</pros>
      <cons>Hyphenless username reads slightly less professionally on `sameAs` recruiter inspection. CONTEXT D-17 wrote the more-professional form.</cons>
    </option>
    <option id="option-b">
      <name>Switch to eren-atalay.github.io</name>
      <pros>Hyphenated form reads as "Eren Atalay" not "erenatalaycs"; recruiter-friendly URL; matches CONTEXT D-17.</pros>
      <cons>Requires GitHub-side rename + repo URL change. `identity.github` + JSON-LD `sameAs` GitHub URL must update. New URL must be propagated. Old `erenatalaycs.github.io` URL would 301-redirect (GitHub handles), but bookmarks Eren has shared (LinkedIn, CV) would need updating.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (stay on erenatalaycs) OR option-b (switch to eren-atalay). State the choice; subsequent tasks propagate.</resume-signal>

  <action>
    Present the checkpoint context above to the user. Wait for the resume signal. Once received, proceed to the next task in this plan (or, if this is the terminal task, run `gsd-sdk query commit` and produce SUMMARY.md per the &lt;output&gt; section).
  </action>
  <verify>
    <automated>echo "Checkpoint task — verification is the user's resume signal: Select: option-a (stay on erenatalaycs) OR option-b (switch to eren-atalay). Sta..." &amp;&amp; true</automated>
  </verify>
  <done>
    User has typed the resume signal documented in &lt;resume-signal&gt; above. Decision / verification recorded in SUMMARY.md at plan completion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace public/sitemap.xml with six-URL form (no changefreq/priority)</name>
  <files>public/sitemap.xml</files>
  <behavior>
    - File replaces the existing 1-URL stub.
    - Six `<url>` entries in this exact order: `/`, `/?view=3d`, `/?focus=projects`, `/?focus=writeups`, `/?focus=certs`, `/?focus=contact`.
    - Each entry has `<loc>` + `<lastmod>` only — NO `<changefreq>`, NO `<priority>` (deprecated by Google 2023 per RESEARCH State of the Art).
    - All `<loc>` values use the canonical host from Task 1.
    - `<lastmod>` is today's ISO date (`YYYY-MM-DD`) — uniform across all six entries.
  </behavior>
  <action>
    Read the canonical host decided in Task 1 (`HOST = erenatalaycs.github.io` or `eren-atalay.github.io`). Rewrite `public/sitemap.xml` exactly per RESEARCH Code Examples § sitemap.xml:

    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
      <!-- Source: 04-CONTEXT.md § Claude's Discretion (sitemap content);
           04-UI-SPEC.md § SEO surfaces; 04-RESEARCH.md State of the Art -->
      <url><loc>https://${HOST}/Portfolio_Website/</loc><lastmod>${TODAY}</lastmod></url>
      <url><loc>https://${HOST}/Portfolio_Website/?view=3d</loc><lastmod>${TODAY}</lastmod></url>
      <url><loc>https://${HOST}/Portfolio_Website/?focus=projects</loc><lastmod>${TODAY}</lastmod></url>
      <url><loc>https://${HOST}/Portfolio_Website/?focus=writeups</loc><lastmod>${TODAY}</lastmod></url>
      <url><loc>https://${HOST}/Portfolio_Website/?focus=certs</loc><lastmod>${TODAY}</lastmod></url>
      <url><loc>https://${HOST}/Portfolio_Website/?focus=contact</loc><lastmod>${TODAY}</lastmod></url>
    </urlset>
    ```

    Substitute `${HOST}` with the chosen handle and `${TODAY}` with the actual ISO date the plan runs (e.g. `2026-05-15`). Save. `npm run build` copies `public/` verbatim to `dist/`.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test "$(grep -c '&lt;loc&gt;' public/sitemap.xml)" -eq 6 &amp;&amp; grep -F '/?focus=contact' public/sitemap.xml &amp;&amp; ! grep -E '&lt;changefreq&gt;|&lt;priority&gt;' public/sitemap.xml &amp;&amp; test "$(grep -c '&lt;loc&gt;' dist/sitemap.xml)" -eq 6</automated>
  </verify>
  <done>
    `public/sitemap.xml` lists exactly 6 URLs with `<loc>` + `<lastmod>` only. No deprecated tags. Survives into `dist/sitemap.xml` after build.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile host across index.html + robots.txt + verify JSON-LD email omission + lock copy from UI-SPEC</name>
  <files>index.html, public/robots.txt</files>
  <behavior>
    - All six host references in `index.html` use the canonical host from Task 1: `<link rel="canonical">`, `<meta property="og:url">`, `<meta property="og:image">`, `<meta name="twitter:image">`, JSON-LD `"url"`, JSON-LD `"image"`.
    - JSON-LD `sameAs` GitHub URL matches the canonical username (Option A: `https://github.com/erenatalaycs`; Option B: `https://github.com/eren-atalay`).
    - `<title>` matches UI-SPEC verbatim: `Eren Atalay — Junior Cybersecurity Analyst (UK)` (with `(UK)` suffix). em-dash is literal U+2014.
    - `<meta name="description">` matches UI-SPEC verbatim: `Junior cybersecurity analyst from the UK — CV, projects, CTF write-ups, and a 3D hacker workstation portfolio.` (em-dash literal U+2014, ≤155 chars).
    - Twitter title + description mirror og:title + og:description.
    - JSON-LD does NOT contain `"email"` field (Phase 1 OPSEC carry-over — verified by grep, NOT changed).
    - Existing CSP `<meta>` is UNCHANGED (Plan 04-02 already extended it for Web3Forms).
    - `public/robots.txt` `Sitemap:` line uses the canonical host.
  </behavior>
  <action>
    Step 1 — Read `index.html`. Replace `erenatalaycs` (or `eren-atalay`, whichever is the OUTGOING form) with the canonical chosen host, in:
    - `<link rel="canonical" href="https://${HOST}/Portfolio_Website/" />`
    - `<meta property="og:url" content="https://${HOST}/Portfolio_Website/" />`
    - `<meta property="og:image" content="https://${HOST}/Portfolio_Website/assets/og-image.png" />`
    - `<meta name="twitter:image" content="https://${HOST}/Portfolio_Website/assets/og-image.png" />`
    - JSON-LD `"url": "https://${HOST}/Portfolio_Website/"`
    - JSON-LD `"image": "https://${HOST}/Portfolio_Website/assets/og-image.png"`
    - JSON-LD `"sameAs": ["https://github.com/${USERNAME}", "https://www.linkedin.com/in/eren-atalay/"]` — `${USERNAME}` is `erenatalaycs` (Option A) OR `eren-atalay` (Option B)

    Step 2 — Update `<title>` to UI-SPEC verbatim: `<title>Eren Atalay — Junior Cybersecurity Analyst (UK)</title>` (current is `Eren Atalay — Junior Cybersecurity Analyst` — adds `(UK)` suffix).

    Step 3 — Update `<meta name="description">` to UI-SPEC verbatim: `Junior cybersecurity analyst from the UK — CV, projects, CTF write-ups, and a 3D hacker workstation portfolio.` (current is `Eren Atalay — Junior Cybersecurity Analyst (UK). CV, projects, certifications, and home-lab write-ups.`).

    Step 4 — Update `<meta property="og:title">` to match new `<title>`. Update `<meta property="og:description">` to match new meta description. Update `<meta name="twitter:title">` and `<meta name="twitter:description">` likewise.

    Step 5 — VERIFY (do NOT modify): the JSON-LD `<script type="application/ld+json">` block does NOT contain a `"email"` field. The existing JSON-LD has fields: `@context`, `@type`, `name`, `jobTitle`, `url`, `homeLocation`, `sameAs`, `image`. No `email`. The acceptance grep gate enforces this.

    Step 6 — Update the JSON-LD comment to acknowledge Phase 4 verification:
    ```html
    <!-- JSON-LD Person schema. Email is INTENTIONALLY OMITTED — including it
         would defeat the rot13+base64 obfuscation in src/lib/obfuscate.ts.
         See 01-RESEARCH.md Pitfall 4. Phase 4 (CHECKLIST-LAUNCH) verifies
         the omission survives the build. -->
    ```

    Step 7 — Edit `public/robots.txt`. Update `Sitemap:` line to use the canonical host:
    ```
    User-agent: *
    Allow: /
    Sitemap: https://${HOST}/Portfolio_Website/sitemap.xml
    ```

    Step 8 — Run `npm run build && npm run typecheck && npm run lint && npm run format && npm run parity && npm run size`. Verify with grep:
    ```bash
    # Single-host enforcement: only ONE of the two hosts appears anywhere.
    grep -E '(erenatalaycs|eren-atalay)' index.html public/sitemap.xml public/robots.txt
    # Should ONLY show the chosen host. The OTHER host MUST NOT appear.

    # JSON-LD email omission gate.
    sed -n '/application\/ld+json/,/<\/script>/p' index.html | grep -E '"email"\s*:'
    # Should return EMPTY (gate exits non-zero if found).

    # Sitemap survives into dist/.
    test -f dist/sitemap.xml && grep -c '<loc>' dist/sitemap.xml | grep '6'
    ```

    Note: do NOT touch `src/content/identity.ts` `github` field unless Option B was chosen — in which case ALSO update `identity.github` from `https://github.com/erenatalaycs` to `https://github.com/eren-atalay` (this is the only consumer of the GitHub URL outside JSON-LD).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run parity &amp;&amp; npm run size &amp;&amp; grep -F 'application/ld+json' index.html &amp;&amp; ! sed -n '/application\/ld+json/,/&lt;\/script&gt;/p' index.html | grep -E '"email"\s*:' &amp;&amp; grep -F 'og:image' index.html | grep -F 'og-image.png' &amp;&amp; grep -F '&lt;title&gt;Eren Atalay' index.html &amp;&amp; grep -F 'Sitemap:' public/robots.txt &amp;&amp; grep -F 'Allow: /' public/robots.txt</automated>
  </verify>
  <done>
    `index.html` + `public/robots.txt` use the canonical host consistently. JSON-LD email omission verified. Title + description match UI-SPEC verbatim. Twitter card mirrors OG. Build clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static `index.html` ↔ Search engine crawlers | JSON-LD + sitemap consumed by Google/Bing; metadata becomes recruiter-visible |
| Static `index.html` ↔ Social media unfurl | OG tags consumed by LinkedIn/Slack/Twitter previews |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | Plaintext email leaked via JSON-LD `"email"` field (Pitfall 4) | mitigate | Phase 1 D-09 omits the field; Phase 4 acceptance grep `! sed ... \| grep '"email"'` enforces the omission survives; CHECKLIST-LAUNCH grep gate (Plan 04-07) re-verifies in `dist/index.html` post-build |
| T-04-04-02 | Spoofing | Mismatched canonical/og:url across surfaces enables cache-poisoning via stale OG cards | mitigate | Single-host enforcement: Task 3 grep verifies only ONE host appears across `index.html`/`sitemap.xml`/`robots.txt`. The OUTGOING host MUST NOT remain anywhere. |
| T-04-04-03 | Information Disclosure | OG image binary contains EXIF / XMP / GPS / employer leak | mitigate | OG image BINARY swap is OUT OF SCOPE for this plan — handled by Plan 04-08 with full-resolution OPSEC review per RESEARCH Pattern 12. exiftool CI gate already strips metadata from `public/assets/*` (deploy.yml lines 60-104). |
| T-04-04-04 | Tampering | Sitemap URL list drifts from actual section list; bots crawl 404s | mitigate | The six URLs match SECTIONS in `src/content/sections.ts` + the FocusId canonical URL values (left/center/right → projects/about-or-whoami/writeups + certs + contact). Phase 1 + 3 already verified these resolve. |
</threat_model>

<verification>
**Per-task verification:** see each task's `<verify>` block.

**Plan-level verification:**
1. ONE host appears across `index.html` + `public/sitemap.xml` + `public/robots.txt` (the OTHER must be entirely absent — verified by `grep -E '(erenatalaycs|eren-atalay)'` and confirming only one match).
2. `dist/sitemap.xml` has exactly 6 `<loc>` entries; no `<changefreq>` / `<priority>`.
3. `dist/index.html` JSON-LD does NOT contain `"email":` field.
4. `<title>` and `<meta name="description">` match UI-SPEC § SEO surfaces verbatim.
5. CSP `<meta>` from Plan 04-02 still includes `https://api.web3forms.com` (no regression).
6. All CI gates green: typecheck, parity, build, size-limit.
</verification>

<success_criteria>
- Task 1 checkpoint resolved: ONE canonical GH-Pages host chosen.
- `public/sitemap.xml` ships six URLs, no deprecated tags, canonical host throughout.
- `public/robots.txt` `Sitemap:` line uses canonical host.
- `index.html` SEO meta tags reconciled to canonical host across canonical/og:url/og:image/twitter:image/JSON-LD.
- Title + description match UI-SPEC verbatim.
- JSON-LD continues to omit `email` field (Phase 1 OPSEC carry-over verified).
- Build clean; size-limit unchanged; parity green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-asset-postprocessing-polish-pre-launch-qa/04-04-SUMMARY.md` documenting:
- Which canonical host Eren chose (Option A or B). Whether GitHub username changed.
- The single-host grep verification result.
- The JSON-LD email-omission grep verification result.
- Note that OG image BINARY swap (real screenshot + OPSEC review) is deferred to Plan 04-08 per RESEARCH Pattern 12.
</output>
