---
phase: 03-content-integration-mdx-write-ups-camera-choreography
plan: 06
type: execute
wave: 5
depends_on: [05]
files_modified:
  - src/content/writeups/lolbin-sigma-detection.mdx
  - src/content/writeups/hash-to-attack-triage.mdx
  - src/content/writeups/jwt-algorithm-confusion.mdx
  - public/assets/writeups/lolbin-sigma-detection/
  - public/assets/writeups/hash-to-attack-triage/
  - public/assets/writeups/jwt-algorithm-confusion/
  - src/content/attack-techniques.ts
autonomous: false
requirements: [CNT-02, CNT-03]
must_haves:
  truths:
    - "Three MDX write-ups exist in `src/content/writeups/` with valid frontmatter (all required fields per D-14) and ≥500 words of real (non-placeholder) content each."
    - "Each write-up follows its D-15 type-specific section ordering (Detection: Hypothesis→Telemetry→Sigma Rule→Splunk Test→False Positives→Lessons; CTI: Sample Selection→IOC Extraction→ATT&CK Mapping→Detection Rule→Lessons; Web Auth: Vulnerability Class→Reproduction→Exploit→Fix→Lessons)."
    - "Every screenshot in `public/assets/writeups/<slug>/` passes the OPSEC checklist (no IPs, hostnames, employer terms, file paths, live CTF flags) AND has been EXIF-stripped."
    - "Every `<ScreenshotFrame>` carries `n`, `src`, `alt`, `caption` props; alt is real (not 'screenshot') per WCAG."
    - "Every write-up cites `sources` and `attack_techniques` in frontmatter; `<ProvenanceFooter>` (auto-mounted) renders citations + ATT&CK links + author + date."
    - "All three writeups appear in `<WriteupList>` (3D shell right monitor) AND in `<Writeups />` (text shell index) automatically — `import.meta.glob` picks them up; no consumer code changes."
    - "tsc + eslint + prettier + vitest + npm run build all green after each write-up; size-limit budgets respected (text <120 KB gz, 3D <450 KB gz)."
    - "No plagiarism: every quoted command, rule, or excerpt cites the originating source (SigmaHQ, Atomic Red Team, MalwareBazaar, PortSwigger Web Security Academy, etc.)."
  artifacts:
    - path: "src/content/writeups/lolbin-sigma-detection.mdx"
      provides: "Detection Engineering write-up (~12 min read). Detects certutil.exe -urlcache LOLBin abuse via Sigma rule + Splunk validation. Type='detection'. Sections per D-15. Cites SigmaHQ + Atomic Red Team. ATT&CK: T1140, T1105, T1218.010. disclaimer='home-lab'."
      min_lines: 80
    - path: "src/content/writeups/hash-to-attack-triage.mdx"
      provides: "Threat Intelligence write-up (~15 min read). Triages a public retired sample from MalwareBazaar; extracts IOCs; maps TTPs to MITRE ATT&CK; writes Sigma + YARA detection rules. Type='cti'. Sections per D-15. Cites MalwareBazaar + VirusTotal + Hybrid Analysis."
      min_lines: 80
    - path: "src/content/writeups/jwt-algorithm-confusion.mdx"
      provides: "Web Auth write-up (~10 min read). Reproduces HS256/RS256 confusion + alg=none on PortSwigger Web Security Academy lab; demonstrates forging admin JWT; writes Express.js fix. Type='web-auth'. Sections per D-15. Cites PortSwigger lab. ATT&CK: T1190."
      min_lines: 80
    - path: "public/assets/writeups/lolbin-sigma-detection/"
      provides: "1-3 EXIF-stripped, OPSEC-reviewed screenshots (Splunk dashboard, Sigma rule diff, false-positive baseline). All flagged `[✓ sanitized]` in MDX captions."
    - path: "public/assets/writeups/hash-to-attack-triage/"
      provides: "1-3 EXIF-stripped, OPSEC-reviewed screenshots (VirusTotal sandbox graph, ATT&CK Navigator layer, IOC list). All flagged `[✓ sanitized]`."
    - path: "public/assets/writeups/jwt-algorithm-confusion/"
      provides: "0-2 EXIF-stripped, OPSEC-reviewed screenshots (forged JWT decode in jwt.io / Burp Repeater). May have 0 if the forged JWT is more compelling rendered as a CodeBlock than as a screenshot."
    - path: "src/content/attack-techniques.ts"
      provides: "EXTENDED if any write-up references a technique not in Plan 05's seed list. Each new entry adds key + canonical name from attack.mitre.org."
  key_links:
    - from: "src/content/writeups/*.mdx"
      to: "src/content/writeups.ts barrel"
      via: "import.meta.glob('./writeups/*.mdx', { eager: true })"
      pattern: "import\\.meta\\.glob"
    - from: "src/content/writeups/*.mdx"
      to: "<ScreenshotFrame> + <CodeBlock> MDX components"
      via: "MDXProvider components map (Plan 05 wired)"
      pattern: "ScreenshotFrame|CodeBlock"
    - from: "src/content/writeups/*.mdx frontmatter"
      to: "<ProvenanceFooter> auto-mount"
      via: "WriteupView reads frontmatter and passes to ProvenanceFooter"
      pattern: "attack_techniques"
---

<objective>
Author 3 production-ready MDX write-ups (CNT-02 — second half) and resolve Plan 03-05's empty-glob state. Each write-up is authored COLLABORATIVELY between Eren (subject expertise + lab attempts + OPSEC review) and Claude (structure, ATT&CK mapping, Sigma/YARA syntax verification, prose tightening, sanitization review). After each write-up: tsc + eslint + prettier + vitest + build green; size-limit green; OPSEC checklist signed off; HUMAN-VERIFY checkpoint pauses the plan for Eren's approval.

Purpose: this plan retires the content-authoring risk — Pitfall 7 (junior authenticity → provenance footer mandatory + plagiarism check), Pitfall 5/10 (OPSEC slip in screenshots → exiftool + manual full-resolution review + black-box redaction), Pitfall 8 (ATT&CK technique not in lookup table → ProvenanceFooter shows fallback "(unknown technique)" → Plan 05 mitigates by extending attack-techniques.ts during authoring), Pitfall 9 (frontmatter drift → assertFrontmatter throws at import time). Plan 06 ships 3 deliberately diverse subjects (Detection / CTI / Web Auth) so the recruiter sees defensive + analytical + offensive breadth, all with Pattern 7 mitigations baked in.

Output: 3 MDX files (~80-120 lines each) + 3-9 sanitized PNG screenshots + possible attack-techniques.ts extension + 3 atomic commits (1 per write-up). Each write-up is its own checkpoint:human-verify task because content authoring requires Eren's domain expertise, sanitization review, and final approval before commit. The plan's 3 tasks run sequentially (one write-up at a time) so OPSEC review is focused.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/CHECKLIST-OPSEC.md
@.planning/phases/03-content-integration-mdx-write-ups-camera-choreography/03-CONTEXT.md
@.planning/phases/03-content-integration-mdx-write-ups-camera-choreography/03-RESEARCH.md
@.planning/phases/03-content-integration-mdx-write-ups-camera-choreography/03-UI-SPEC.md
@.planning/phases/03-content-integration-mdx-write-ups-camera-choreography/03-05-SUMMARY.md
@src/content/writeups.ts
@src/content/attack-techniques.ts
@src/ui/MDXRenderer.tsx
@src/ui/CodeBlock.tsx
@src/ui/ScreenshotFrame.tsx
@src/ui/ProvenanceFooter.tsx

<interfaces>
<!-- D-14 frontmatter schema (writeups.ts assertFrontmatter enforces non-empty title/slug/type/date/duration/sources/attack_techniques) -->
title: string                           # full sentence — recruiter-readable
slug: string                            # URL-safe; matches filename (e.g. lolbin-sigma-detection)
type: 'detection' | 'cti' | 'web-auth'
date: 'YYYY-MM-DD'                      # ISO; sort key for writeups[]
duration: '~N min read'                 # author estimate
tags: string[]                          # lowercase; Plan 07's parity audit pairs against skills.ts
sources: string[]                       # each "Display Name: https://url" — colon-space delimiter
attack_techniques: string[]             # technique IDs (e.g. 'T1218.010') — must exist in attack-techniques.ts (Pitfall 8)
disclaimer?: 'home-lab'                 # only when applicable; renders disclaimer line in ProvenanceFooter
author?: string                         # defaults to identity.name in ProvenanceFooter

<!-- D-15 per-type section ordering (sections render via MDX h2 → WriteupH2; ProvenanceFooter auto-mounts at the end) -->
detection: Hypothesis → Telemetry → Sigma Rule → Splunk Test → False Positives → Lessons
cti:       Sample Selection → IOC Extraction → ATT&CK Mapping → Detection Rule → Lessons
web-auth:  Vulnerability Class → Reproduction → Exploit → Fix → Lessons

<!-- MDX usage shapes -->
<CodeBlock>          ← rehype-pretty-code's ` ```yaml title="filename" {3,5} `, wraps the figure
                       Author writes ` ```yaml title="lolbin_certutil.yml" {3,5} ` ... ` ``` `; figure override applies chrome
                       Title meta and line-highlight syntax both supported (rehype-pretty-code).
<ScreenshotFrame n={1} src="<slug>/01-name.png" alt="..." caption="..." />   ← src relative to public/assets/writeups/
<ProvenanceFooter />  ← AUTO-MOUNTED by WriteupView. AUTHORS DO NOT INCLUDE IT.

<!-- Inline code: backticks `certutil.exe` render with bg-surface-1 + body color (no Shiki tokens) -->

<!-- Forbidden in MDX bodies (UI-SPEC + REQUIREMENTS.md OOS) -->
NO inline <script> or <style>          (security)
NO external CDN images (only public/assets/writeups/<slug>/*.png)
NO target="_blank" on row links        (Plan 05 BracketLink already handles)
NO "Read more" / "Click to read" patronising copy
NO type-out animation on body content
NO live CTF flags                     (redact with BLACK BOX, not blur)
NO IPs / hostnames / employer terms / file paths revealing real-name folders
NO plagiarism: every command, rule, excerpt cites originating source
NO custom Shiki theme JSON
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Author Detection Engineering write-up — "Detecting certutil.exe Network Abuse with Splunk + Sigma"</name>
  <read_first>
    - .planning/phases/03-content-integration-mdx-write-ups-camera-choreography/03-CONTEXT.md (D-15 detection section ordering; D-17 subject 1; D-18 collaborative authoring approach)
    - .planning/phases/03-content-integration-mdx-write-ups-camera-choreography/03-UI-SPEC.md (§ Per-type section structure D-15 → Detection write-up; Code blocks + screenshots; <ScreenshotFrame> caption format)
    - .planning/phases/03-content-integration-mdx-write-ups-camera-choreography/03-RESEARCH.md (Example 1 — full Detection write-up MDX shape)
    - .planning/CHECKLIST-OPSEC.md (Screenshot Review section — every PNG runs through this gate before commit)
    - src/content/attack-techniques.ts (verify T1140, T1105, T1218.010 already present from Plan 05)
  </read_first>
  <what-built>
    Eren and Claude collaboratively author `src/content/writeups/lolbin-sigma-detection.mdx` covering:
    - **Subject**: Detect `certutil.exe -urlcache -f <url> <output>` LOLBin abuse via Sigma rule + Splunk validation against synthetic Sysmon events.
    - **Frontmatter** (D-14): title, slug=`lolbin-sigma-detection`, type=`detection`, date (today's ISO), duration=`'~12 min read'`, tags=`[splunk, sigma, lolbin, certutil, detection-engineering, sysmon]`, sources=`["SigmaHQ rules: https://github.com/SigmaHQ/sigma", "Atomic Red Team: https://github.com/redcanaryco/atomic-red-team"]`, attack_techniques=`['T1140', 'T1105', 'T1218.010']`, disclaimer=`'home-lab'`.
    - **Sections (D-15 detection ordering, render as MDX h2)**:
      1. `## Hypothesis` — what behaviour we're detecting and why suspicious (~80 words)
      2. `## Telemetry` — log sources / Sysmon EID / Splunk index + sourcetype (~80 words; may include a small markdown table from remark-gfm)
      3. `## Sigma Rule` — full annotated Sigma rule wrapped in `<CodeBlock>` ` ```yaml title="lolbin_certutil.yml" {3,5} ` ... ` ``` ` — author writes the rule body (Eren); Claude verifies Sigma syntax (modifiers, condition, level)
      4. `## Splunk Test` — synthetic event ingest steps + the `tstats` / SPL search query in a code block + ScreenshotFrame for the Splunk dashboard match (~120 words)
      5. `## False Positives` — known-good behaviour that triggers (e.g., legitimate admin running `certutil` for cert install) + tuning approach (~80 words)
      6. `## Lessons` — what went well, what was harder than expected (~80 words). Honest junior framing (Pitfall 7) — NOT "I cracked the SOC playbook"; instead "first time writing a Sigma rule against synthetic events; learned to test FP rates BEFORE deploying."
    - **Screenshots (1-3)**:
      - At minimum 1: Splunk dashboard showing the rule-match against synthetic Sysmon events, full-resolution OPSEC-reviewed (NO IPs, hostnames, employer terms, file paths). Filename: `01-splunk-dashboard.png` in `public/assets/writeups/lolbin-sigma-detection/`.
      - Optional: 2nd showing the Sigma rule diff or false-positive baseline.
    - **Provenance**: <ProvenanceFooter /> auto-renders the 2 sources + 3 ATT&CK techniques + author + date + home-lab disclaimer.
    - Total ≥500 words of real content (recruiter who reads it should think "this person actually wrote a Sigma rule and tested it").
  </what-built>
  <how-to-verify>
    Run these CHECKS in order; pause if any fails for Eren to refine before continuing:

    1. **Frontmatter validation**: `node -e "import('./src/content/writeups.ts').then(m => console.log(m.writeups.length, m.writeups[0]?.title))"` — should print `1 Detecting certutil.exe Network Abuse...` (or 1 if this is the first write-up). If assertFrontmatter throws, fix the missing field.
    2. **Section ordering**: open the MDX file and verify ## headings appear in the D-15 detection order: Hypothesis, Telemetry, Sigma Rule, Splunk Test, False Positives, Lessons. NO `<ProvenanceFooter />` in the body (it auto-mounts).
    3. **Word count**: `wc -w src/content/writeups/lolbin-sigma-detection.mdx` ≥ 500 (frontmatter is excluded by spirit; the body must carry real content, not stubs).
    4. **Sigma rule syntax**: open the rule code block; verify it has `title:`, `id:` (UUID), `status:`, `description:`, `logsource:`, `detection:` with at least one selection block + `condition:`, `level:`. Run a syntax check by pasting into https://uncoder.io or sigmac if tooling available locally.
    5. **OPSEC checklist (run for EACH PNG in `public/assets/writeups/lolbin-sigma-detection/`)**:
       - [ ] `exiftool -all= -P -overwrite_original public/assets/writeups/lolbin-sigma-detection/*.png` succeeded
       - [ ] `exiftool` verify shows no Author / GPS / CreatorTool / Software metadata
       - [ ] Manual full-resolution review: NO IPs (any `\d+\.\d+\.\d+\.\d+` pattern), NO hostnames (`*.local`, `*.lan`, `*.internal`), NO file paths revealing `/Users/erenatalay` or similar, NO employer terms (`Fin-On`, employer brand names), NO browser tab titles leaking other contexts, NO Slack/Teams/email notifications in corner
       - [ ] If a redaction was needed: BLACK BOX (not blur — blur reversible per Pitfall 5)
       - [ ] CTF box / event closed (per platform rules) and write-up permitted
       - [ ] `[✓ sanitized]` badge present in MDX caption (auto by ScreenshotFrame, but verify it actually rendered)
    6. **No plagiarism**: every command shown, every Sigma field name, every Splunk SPL fragment cites or links to its origin (SigmaHQ rules, Splunk docs, Atomic Red Team). The Sigma rule itself can be Eren's authored work; if adapted from a SigmaHQ rule, frontmatter `sources` cites the originating rule + commit hash.
    7. **ATT&CK technique check**: every id in `attack_techniques` exists as a key in `src/content/attack-techniques.ts`. Run `node -e "..."` or grep. If missing → add to attack-techniques.ts in this same task (don't defer).
    8. **CI gate suite**: `npx tsc --noEmit && npx eslint . --max-warnings=0 && npx prettier --check . && npm test && npm run build && npx size-limit` — ALL exit 0.
    9. **Bundle size delta**: 3D chunk now contains the compiled MDX module + components map; should grow ~3-5 KB gz vs Plan 05 baseline. Still <450 KB gz. Text-shell entry chunk size unchanged (Pattern 7 Option A: MDX runtime not in entry).
    10. **Visual smoke** (Eren's local dev): `npm run dev`, navigate to `?view=3d`, click right monitor, verify the write-up appears in the list with correct type marker `[D]`, click the row, verify WriteupView renders MDX correctly — Sigma rule code block has filename caption + `[copy]` button + Shiki tokens; ScreenshotFrame shows the bordered image + `[✓ sanitized]` badge; ProvenanceFooter at the bottom shows sources + ATT&CK links + meta + home-lab disclaimer. Click `[← back to list]` returns to the index.
    11. **Text shell cross-check**: navigate to `?view=text&focus=writeups/lolbin-sigma-detection` — same MDX renders inside the lazy WriteupsRoute. Check Network tab: WriteupsRoute-*.js chunk loaded; entry chunk did NOT grow.

    Approve `[approved]` once ALL checks pass; describe any issue otherwise. After approval, commit with message: `feat(03-06): author Detection write-up — certutil LOLBin Sigma rule + Splunk validation`. Stage:
    - src/content/writeups/lolbin-sigma-detection.mdx
    - public/assets/writeups/lolbin-sigma-detection/*.png
    - src/content/attack-techniques.ts (only if extended in step 7)
  </how-to-verify>
  <resume-signal>Type "approved" once all 11 checks above pass and the OPSEC checklist is signed off; describe issues if any check fails.</resume-signal>
  <verify>
    <automated>
      ls src/content/writeups/lolbin-sigma-detection.mdx &&
      grep -F "type: detection" src/content/writeups/lolbin-sigma-detection.mdx &&
      grep -F "slug: lolbin-sigma-detection" src/content/writeups/lolbin-sigma-detection.mdx &&
      grep -F "T1218.010" src/content/writeups/lolbin-sigma-detection.mdx &&
      grep -F "T1140" src/content/writeups/lolbin-sigma-detection.mdx &&
      grep -F "T1105" src/content/writeups/lolbin-sigma-detection.mdx &&
      grep -F "disclaimer: home-lab" src/content/writeups/lolbin-sigma-detection.mdx &&
      grep -F "## Hypothesis" src/content/writeups/lolbin-sigma-detection.mdx &&
      grep -F "## Telemetry" src/content/writeups/lolbin-sigma-detection.mdx &&
      grep -F "## Sigma Rule" src/content/writeups/lolbin-sigma-detection.mdx &&
      grep -F "## Splunk Test" src/content/writeups/lolbin-sigma-detection.mdx &&
      grep -F "## False Positives" src/content/writeups/lolbin-sigma-detection.mdx &&
      grep -F "## Lessons" src/content/writeups/lolbin-sigma-detection.mdx &&
      ! grep -F "<ProvenanceFooter" src/content/writeups/lolbin-sigma-detection.mdx &&
      ! grep -F "<script" src/content/writeups/lolbin-sigma-detection.mdx &&
      ! grep -F "192.168." src/content/writeups/lolbin-sigma-detection.mdx &&
      ! grep -F "10.0.0." src/content/writeups/lolbin-sigma-detection.mdx &&
      ! grep -F "/Users/" src/content/writeups/lolbin-sigma-detection.mdx &&
      grep -F "T1218.010" src/content/attack-techniques.ts &&
      grep -F "T1140" src/content/attack-techniques.ts &&
      grep -F "T1105" src/content/attack-techniques.ts &&
      [ "$(wc -w < src/content/writeups/lolbin-sigma-detection.mdx)" -ge 500 ] &&
      ls public/assets/writeups/lolbin-sigma-detection/ | grep -E '\.png$' &&
      npx tsc --noEmit &&
      npx eslint . --max-warnings=0 &&
      npx prettier --check . &&
      npm test &&
      npm run build &&
      ! grep -F "MDXProvider" dist/assets/index-*.js &&
      grep -F "MDXProvider" dist/assets/ThreeDShell-*.js &&
      npx size-limit
    </automated>
  </verify>
  <acceptance_criteria>
    - lolbin-sigma-detection.mdx exists with valid D-14 frontmatter (verified by assertFrontmatter not throwing during build).
    - All 6 D-15 detection sections present in correct order (Hypothesis, Telemetry, Sigma Rule, Splunk Test, False Positives, Lessons).
    - ≥500 words of real content (no placeholder TODO comments in body).
    - 3 ATT&CK techniques (T1140, T1105, T1218.010) ALL exist as keys in attack-techniques.ts.
    - At least 1 PNG in public/assets/writeups/lolbin-sigma-detection/, EXIF-stripped, OPSEC-reviewed.
    - No plagiarism (every command/rule/excerpt cites source).
    - No `<ProvenanceFooter />` in MDX body (auto-mount).
    - No inline `<script>`, no real IPs, no hostnames, no `/Users/` paths in MDX or PNGs.
    - tsc/eslint/prettier/test/build all green.
    - Pattern 7 Option A holds: MDXProvider in 3D chunk, NOT in entry chunk.
    - size-limit green.
    - Eren signs off on OPSEC checklist for each PNG.
  </acceptance_criteria>
  <done>
    Detection write-up shipped with full provenance, 3 ATT&CK techniques, ≥500 words, OPSEC-reviewed screenshot(s), atomic commit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Author Threat Intelligence write-up — "From Hash to ATT&CK: Triaging a Malware Sample"</name>
  <read_first>
    - .planning/phases/03-content-integration-mdx-write-ups-camera-choreography/03-CONTEXT.md (D-15 cti section ordering; D-17 subject 2)
    - .planning/phases/03-content-integration-mdx-write-ups-camera-choreography/03-UI-SPEC.md (§ Per-type section structure D-15 → CTI write-up)
    - src/content/writeups/lolbin-sigma-detection.mdx (use as a reference for tone/structure — DO NOT copy content)
    - .planning/CHECKLIST-OPSEC.md
    - src/content/attack-techniques.ts (Plan 05 + Plan 06 Task 1 may have extended; verify CTI techniques present)
  </read_first>
  <what-built>
    Eren picks ONE retired public sample from MalwareBazaar (https://bazaar.abuse.ch/) — selection criterion: family is ATT&CK-mapped on the public threat report and the sample has a non-empty MalwareBazaar comment thread for context. Co-authors `src/content/writeups/hash-to-attack-triage.mdx`:
    - **Frontmatter**: title=`'From Hash to ATT&CK: Triaging a Malware Sample'`, slug=`hash-to-attack-triage`, type=`cti`, date (today), duration=`'~15 min read'`, tags=`[mitre-att&ck, malware-bazaar, virustotal, hybrid-analysis, sigma, yara, threat-intel]`, sources (cite EVERY tool's URL Eren consulted: MalwareBazaar sample page, VirusTotal report, Hybrid Analysis sandbox report, any walkthroughs), attack_techniques (3-5 from the sample's TTP analysis; Plan 06 Task 2 extends attack-techniques.ts if any are new). disclaimer=`'home-lab'` (sample run in home VM).
    - **Sections (D-15 cti ordering)**:
      1. `## Sample Selection` — which family + hash + why this sample (retired enough to discuss publicly; ATT&CK-mapped) (~80 words)
      2. `## IOC Extraction` — hashes (SHA256), C2 domains, mutexes, file paths created. Author may use a small markdown table for IOC type → value (~120 words)
      3. `## ATT&CK Mapping` — TTPs observed mapped to MITRE techniques. Each mapping cites which IOC / behaviour led to the technique inference. (~120 words)
      4. `## Detection Rule` — Sigma + YARA rules in `<CodeBlock>` pairs. Author writes; Claude verifies syntax (Sigma: modifiers, condition; YARA: meta, strings, condition). (~120 words)
      5. `## Lessons` — honest junior framing (~80 words). E.g., "VirusTotal verdict was 30/70 — Hybrid Analysis sandbox was the deciding signal; learned to weight dynamic over static when verdicts diverge."
    - **Screenshots (1-3)**:
      - VirusTotal sandbox graph or detection summary (sanitised tab title — close everything else first, redact submission ID)
      - ATT&CK Navigator layer JSON exported as image
      - IOC list (could be a CodeBlock in MDX instead — author's call)
      - Filenames: `01-virustotal-sandbox.png`, `02-att-and-ck-layer.png`, etc., in `public/assets/writeups/hash-to-attack-triage/`.
    - **Provenance**: every IOC, every TTP, every detection rule citation in frontmatter `sources` so ProvenanceFooter renders them.
    - Total ≥500 words.
  </what-built>
  <how-to-verify>
    Run the same 11-step checklist as Task 1, adjusted for CTI:

    1. Frontmatter valid (assertFrontmatter doesn't throw on build).
    2. Section ordering matches D-15 cti: Sample Selection → IOC Extraction → ATT&CK Mapping → Detection Rule → Lessons.
    3. Word count ≥ 500.
    4. Sigma + YARA rule syntax checks: Sigma uncoder/sigmac if available; YARA: yarac --no-includes --fail-on-warnings if available, else manual review for `meta:`, `strings:`, `condition:` block. If tooling unavailable, Eren confirms by hand.
    5. **OPSEC checklist for EACH PNG** — extra vigilance for CTI:
       - VirusTotal screenshots: redact your submission email (top-right), tab titles showing "VirusTotal — <other-engagement>"
       - ATT&CK Navigator: confirm only the public ATT&CK matrix shown, no private layer URLs
       - IOC screenshots: if showing a sandbox host IP/MAC, redact with BLACK BOX
       - All other items from Task 1 step 5
    6. **No plagiarism / sample disclosure compliance**: the sample family is RETIRED publicly disclosed; the IOCs come from public sources (MalwareBazaar comment, VirusTotal community discussion, Krebs/etc. blog post if relevant — cite). NO uncited IOCs.
    7. **ATT&CK lookup**: every technique id in `attack_techniques` exists in attack-techniques.ts. EXTEND if needed.
    8. CI gate suite green.
    9. Bundle delta (~3-5 KB gz to 3D chunk; entry chunk unchanged).
    10. Visual smoke in 3D shell — type marker `[I]` for `cti`.
    11. Text shell cross-check — `?focus=writeups/hash-to-attack-triage`.

    Approve once all checks pass. Commit message: `feat(03-06): author CTI write-up — sample triage to ATT&CK mapping with Sigma+YARA detection`. Stage: src/content/writeups/hash-to-attack-triage.mdx, public/assets/writeups/hash-to-attack-triage/*.png, src/content/attack-techniques.ts (if extended).
  </how-to-verify>
  <resume-signal>Type "approved" once all 11 checks above pass; describe issues otherwise.</resume-signal>
  <verify>
    <automated>
      ls src/content/writeups/hash-to-attack-triage.mdx &&
      grep -F "type: cti" src/content/writeups/hash-to-attack-triage.mdx &&
      grep -F "slug: hash-to-attack-triage" src/content/writeups/hash-to-attack-triage.mdx &&
      grep -F "## Sample Selection" src/content/writeups/hash-to-attack-triage.mdx &&
      grep -F "## IOC Extraction" src/content/writeups/hash-to-attack-triage.mdx &&
      grep -F "## ATT&CK Mapping" src/content/writeups/hash-to-attack-triage.mdx &&
      grep -F "## Detection Rule" src/content/writeups/hash-to-attack-triage.mdx &&
      grep -F "## Lessons" src/content/writeups/hash-to-attack-triage.mdx &&
      ! grep -F "<ProvenanceFooter" src/content/writeups/hash-to-attack-triage.mdx &&
      ! grep -F "<script" src/content/writeups/hash-to-attack-triage.mdx &&
      ! grep -F "192.168." src/content/writeups/hash-to-attack-triage.mdx &&
      ! grep -F "/Users/" src/content/writeups/hash-to-attack-triage.mdx &&
      [ "$(wc -w < src/content/writeups/hash-to-attack-triage.mdx)" -ge 500 ] &&
      ls public/assets/writeups/hash-to-attack-triage/ | grep -E '\.png$' &&
      npx tsc --noEmit &&
      npx eslint . --max-warnings=0 &&
      npx prettier --check . &&
      npm test &&
      npm run build &&
      ! grep -F "MDXProvider" dist/assets/index-*.js &&
      npx size-limit
    </automated>
  </verify>
  <acceptance_criteria>
    - hash-to-attack-triage.mdx exists with valid D-14 frontmatter.
    - All 5 D-15 cti sections in correct order.
    - ≥500 words.
    - 3-5 ATT&CK techniques referenced; each in attack-techniques.ts.
    - 1+ OPSEC-reviewed PNGs in public/assets/writeups/hash-to-attack-triage/.
    - Every IOC + TTP cited in frontmatter sources.
    - No plagiarism.
    - tsc/eslint/prettier/test/build all green.
    - size-limit green.
    - Eren signs off OPSEC.
  </acceptance_criteria>
  <done>
    CTI write-up shipped with full provenance, ATT&CK mapping, Sigma + YARA rules, OPSEC-reviewed screenshots, atomic commit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Author Web Auth write-up — "JWT Algorithm Confusion in Practice"</name>
  <read_first>
    - .planning/phases/03-content-integration-mdx-write-ups-camera-choreography/03-CONTEXT.md (D-15 web-auth section ordering; D-17 subject 3)
    - .planning/phases/03-content-integration-mdx-write-ups-camera-choreography/03-UI-SPEC.md (§ Per-type section structure D-15 → Web Auth write-up)
    - src/content/writeups/lolbin-sigma-detection.mdx, src/content/writeups/hash-to-attack-triage.mdx (reference tone — DO NOT copy)
    - .planning/CHECKLIST-OPSEC.md
    - src/content/attack-techniques.ts (verify T1190 present; Plan 05 seed had it)
  </read_first>
  <what-built>
    Eren reproduces the algorithm-confusion + alg=none lab on PortSwigger Web Security Academy (https://portswigger.net/web-security/jwt). Co-authors `src/content/writeups/jwt-algorithm-confusion.mdx`:
    - **Frontmatter**: title=`'JWT Algorithm Confusion in Practice'`, slug=`jwt-algorithm-confusion`, type=`web-auth`, date, duration=`'~10 min read'`, tags=`[jwt, oauth, web-auth, portswigger, burp-suite]`, sources=`["PortSwigger JWT Lab: https://portswigger.net/web-security/jwt", "RFC 7519 (JWT): https://datatracker.ietf.org/doc/html/rfc7519", "OWASP JWT Cheat Sheet: https://cheatsheetseries.owasp.org/cheatsheets/JSON_Web_Token_for_Java_Cheat_Sheet.html"]`, attack_techniques=`['T1190', 'T1078']`. disclaimer optional (PortSwigger lab is sandboxed; either include or skip — Eren decides — `'home-lab'` is technically applicable since the lab IS a synthetic environment).
    - **Sections (D-15 web-auth ordering)**:
      1. `## Vulnerability Class` — what algorithm confusion is, why HS256/RS256 confusion + alg=none survives in real codebases (~80 words). Cite OWASP cheat sheet.
      2. `## Reproduction` — PortSwigger lab steps, reproducible (~120 words). Burp Repeater flow: capture token → decode → modify alg → resign with public key → re-send.
      3. `## Exploit` — the exploit, with the forged JWT body+sig as a CodeBlock. Optionally a screenshot of Burp Repeater showing the response status flip from 401 → 200. (~120 words)
      4. `## Fix` — the fixed Express.js middleware (or generic fix pattern) in a CodeBlock. Cite a JWT library that gets this right by default (jsonwebtoken with explicit `algorithms: ['RS256']`, jose with `jwtVerify`'s explicit alg option). (~80 words)
      5. `## Lessons` — honest junior framing (~80 words). E.g., "intuited that 'verify' meant 'check signature' but the lib trusted the alg field by default — read RFC 7519 §10.7 to internalise that algs come from configuration, not headers."
    - **Screenshots (0-2)**:
      - Optional: Burp Repeater request/response showing 401 → 200 after exploit.
      - The forged JWT can be a CodeBlock with the 3 dot-separated parts annotated; this may be MORE compelling than a screenshot.
      - Filename: `01-burp-repeater-200.png` if used, in `public/assets/writeups/jwt-algorithm-confusion/`.
    - **Provenance**: cite PortSwigger lab + RFC + OWASP cheat sheet.
    - Total ≥500 words.
  </what-built>
  <how-to-verify>
    Run the same 11-step checklist as Tasks 1 + 2, adjusted for Web Auth:

    1. Frontmatter valid.
    2. Section ordering matches D-15 web-auth: Vulnerability Class → Reproduction → Exploit → Fix → Lessons.
    3. Word count ≥ 500.
    4. JWT exploit verification: the forged JWT decoded on jwt.io shows the `alg=none` or `alg=HS256` (with public key as secret) modification; if shown, the screenshot's Burp Repeater response status MUST be 200 OK and the response body MUST show admin-only content.
    5. **OPSEC checklist for ANY screenshots**:
       - Burp Repeater: redact the JWT (or use a clearly-fake placeholder admin token); redact host (PortSwigger lab URLs are generic but tab title may reveal context), other request history in the side pane
       - jwt.io: similar redactions
       - All other items from Task 1 step 5
    6. **No plagiarism**: PortSwigger Lab is the FIRST source; if Eren walked through their tutorial, cite it explicitly. The fix middleware can be original or adapted from `jsonwebtoken` README — cite if adapted.
    7. ATT&CK lookup: T1190 + T1078 both in attack-techniques.ts (Plan 05 seeded both).
    8. CI gate suite green.
    9. Bundle delta — 3D chunk should now be ~285-295 KB gz (Phase 2 baseline 236.7 + 3 compiled MDX + GSAP from Plan 03 + components from Plan 05). Still <450 KB gz comfortably.
    10. Visual smoke — type marker `[W]` for `web-auth`.
    11. Text shell cross-check.

    **Phase-3-end checks** (run after this final write-up commits):
    12. `<WriteupList>` shows all 3 entries with correct type markers `[D]`, `[I]`, `[W]` and dates in descending order (newest at top per writeups[] sort).
    13. Click each row in 3D shell — all 3 WriteupViews render correctly with full chrome (CodeBlock filename + copy button works, ScreenshotFrame `[✓ sanitized]` badges visible, ProvenanceFooter shows ATT&CK links resolving to mitre.org).
    14. `<Writeups />` text shell section shows all 3 entries; clicking each row triggers the lazy WriteupsRoute chunk (verify in Network tab).
    15. `npm run size` final bundle metrics committed to SUMMARY.

    Approve once all 15 checks pass. Commit message: `feat(03-06): author Web Auth write-up — JWT alg confusion (PortSwigger lab repro + Express fix)`. Stage: src/content/writeups/jwt-algorithm-confusion.mdx, public/assets/writeups/jwt-algorithm-confusion/*.png (if any), src/content/attack-techniques.ts (only if extended).
  </how-to-verify>
  <resume-signal>Type "approved" once all 11 + 4 (phase-end) checks above pass; describe issues otherwise.</resume-signal>
  <verify>
    <automated>
      ls src/content/writeups/jwt-algorithm-confusion.mdx &&
      grep -F "type: web-auth" src/content/writeups/jwt-algorithm-confusion.mdx &&
      grep -F "slug: jwt-algorithm-confusion" src/content/writeups/jwt-algorithm-confusion.mdx &&
      grep -F "## Vulnerability Class" src/content/writeups/jwt-algorithm-confusion.mdx &&
      grep -F "## Reproduction" src/content/writeups/jwt-algorithm-confusion.mdx &&
      grep -F "## Exploit" src/content/writeups/jwt-algorithm-confusion.mdx &&
      grep -F "## Fix" src/content/writeups/jwt-algorithm-confusion.mdx &&
      grep -F "## Lessons" src/content/writeups/jwt-algorithm-confusion.mdx &&
      grep -F "T1190" src/content/writeups/jwt-algorithm-confusion.mdx &&
      ! grep -F "<ProvenanceFooter" src/content/writeups/jwt-algorithm-confusion.mdx &&
      ! grep -F "<script" src/content/writeups/jwt-algorithm-confusion.mdx &&
      [ "$(wc -w < src/content/writeups/jwt-algorithm-confusion.mdx)" -ge 500 ] &&
      [ "$(ls src/content/writeups/*.mdx | wc -l)" -eq 3 ] &&
      grep -F "T1190" src/content/attack-techniques.ts &&
      npx tsc --noEmit &&
      npx eslint . --max-warnings=0 &&
      npx prettier --check . &&
      npm test &&
      npm run build &&
      ! grep -F "MDXProvider" dist/assets/index-*.js &&
      grep -F "MDXProvider" dist/assets/ThreeDShell-*.js &&
      ls dist/assets/WriteupsRoute-*.js &&
      npx size-limit
    </automated>
  </verify>
  <acceptance_criteria>
    - jwt-algorithm-confusion.mdx exists with valid D-14 frontmatter.
    - All 5 D-15 web-auth sections in correct order.
    - ≥500 words.
    - T1190 (and any others) in attack-techniques.ts.
    - 0+ OPSEC-reviewed PNGs (Eren may decide CodeBlock-only is more compelling).
    - All 3 write-ups now exist (`ls src/content/writeups/*.mdx | wc -l` returns 3).
    - No plagiarism (PortSwigger / RFC / OWASP all cited).
    - tsc/eslint/prettier/test/build all green.
    - Pattern 7 Option A holds: MDX in 3D chunk + WriteupsRoute lazy chunk; NOT in entry.
    - size-limit green; 3D chunk well under 450 KB.
    - Eren signs off final OPSEC + visual smoke for all 3 write-ups.
  </acceptance_criteria>
  <done>
    Web Auth write-up shipped; ALL 3 D-17 write-ups complete; both shells render the full index + each WriteupView; provenance + ATT&CK + OPSEC all green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author MDX → React render | Each `*.mdx` file is author-controlled and reviewed at the human-verify checkpoint. MDX 3 sanitises inline `<script>`. CSP `<meta>` (Phase 1 baseline) further restricts. |
| screenshot → public bundle | Every PNG runs through exiftool + manual full-resolution review BEFORE commit. CI re-strips metadata as a safety net. The `[✓ sanitized]` badge is asserted by author and the OPSEC checklist is the gate. |
| sample selection (CTI) → public disclosure | Sample MUST be retired and publicly discussed; cite the threat report. Don't disclose anything not already in public threat intel. |
| PortSwigger lab → public write-up | PortSwigger labs are public; write-ups are encouraged. Cite the lab page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Information Disclosure | Splunk dashboard screenshot reveals home-lab IPs / hostnames → home-lab security exposure + employer NDA risk | mitigate | OPSEC checklist Screenshot Review (manual full-resolution); exiftool metadata strip; black-box redaction (NOT blur); home-lab disclaimer in frontmatter signals synthetic data context. Plan 03-06 task acceptance gates on this. |
| T-03-06-02 | Information Disclosure | CTI write-up reveals an IOC that's still active (not retired) | mitigate | Sample selection criterion explicit: RETIRED publicly-disclosed family. MalwareBazaar's "Reporter" comment thread is the discovery channel. Eren's OPSEC review confirms before commit. |
| T-03-06-03 | Information Disclosure | live CTF flag visible in screenshot → THM/HTB account ban | mitigate | OPSEC checklist § Screenshot Review explicit: BLACK BOX redaction (not blur — Pitfall 5). Flags from currently-live boxes are forbidden in any write-up content (UI-SPEC). |
| T-03-06-04 | Spoofing | author commits an MDX with `[✓ sanitized]` badge but unsanitised PNG | mitigate | OPSEC checklist is the gate; CI exiftool verifies metadata (catches the most common slip — Author/CreatorTool); manual full-resolution review catches visible content (the part CI can't check); the human-verify checkpoint forces explicit Eren sign-off. |
| T-03-06-05 | Tampering | malicious MDX file injects `<script>` to manipulate page | mitigate | MDX 3 sanitises by default; UI-SPEC § Forbidden assets bans inline `<script>`. Acceptance grep `! grep -F "<script" src/content/writeups/*.mdx` after each task. |
| T-03-06-06 | Repudiation | author later disputes provenance | mitigate | every write-up's ProvenanceFooter cites sources verbatim; git commit history per write-up is atomic + signed (if commit signing enabled). |
| T-03-06-07 | Information Disclosure | jwt.io / Burp screenshot reveals the actual JWT secret or host | mitigate | OPSEC review for JWT screenshots: black-box the JWT body (claims) AND signature segment, OR use a placeholder fake JWT for the screenshot. Burp host bar should show only the PortSwigger lab URL (generic). Confirmed in Task 3 acceptance step 5. |
| T-03-06-08 | Plagiarism (career-damaging in cyber) | author copies a SigmaHQ rule verbatim without attribution | mitigate | Pitfall 7 explicit; ProvenanceFooter mandatory; every quoted command/rule/excerpt cites originating source. The acceptance step 6 enforces. |
| T-03-06-09 | Tampering | a contributor adds a Glitch / DOF / SSAO postprocessing reference in MDX | mitigate | Phase 4 territory; Phase 3 anti-pattern audit (UI-SPEC) bans postprocessing references in body content. Plan 03-06 acceptance grep `! grep -F "Bloom\|Scanline\|ChromaticAberration" src/content/writeups/*.mdx` enforces (no Phase 4 leak into Phase 3). |
</threat_model>

<verification>
- All 3 MDX files exist in `src/content/writeups/`.
- Each frontmatter passes assertFrontmatter (build does NOT throw).
- Each MDX has the correct D-15 section ordering for its type.
- Each MDX has ≥500 words.
- Each ATT&CK technique id referenced in frontmatter exists in `src/content/attack-techniques.ts`.
- All PNGs in `public/assets/writeups/<slug>/` are EXIF-stripped (CI verify step from deploy.yml passes).
- No `<script>`, no real IPs, no `/Users/`, no employer terms in MDX bodies.
- npm run build green; size-limit green; Pattern 7 Option A invariants hold.
- 3 atomic commits land in git log (one per write-up).
- Visual smoke pass in both shells; all 3 write-ups reachable from `<WriteupList>` (3D) AND `<Writeups />` (text).
</verification>

<success_criteria>
- 3 production-ready MDX write-ups committed:
  - lolbin-sigma-detection.mdx (Detection — T1140, T1105, T1218.010)
  - hash-to-attack-triage.mdx (CTI — 3-5 techniques)
  - jwt-algorithm-confusion.mdx (Web Auth — T1190 + T1078)
- Each ≥500 words, follows D-15 type-specific section ordering, cites sources + ATT&CK, OPSEC-reviewed screenshots.
- All 3 ATT&CK technique sets fully covered by attack-techniques.ts (no `(unknown technique)` fallback in any rendered footer).
- 3 atomic commits land in git log with descriptive messages.
- Both shells render all 3 write-ups (3D right monitor + text shell `?focus=writeups/<slug>`).
- CI gate suite green at every commit; size-limit green; entry chunk MDX-runtime-free; 3D chunk under 450 KB gz.
- OPSEC checklist signed off for every PNG.
- 3 human-verify checkpoint approvals from Eren.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-integration-mdx-write-ups-camera-choreography/03-06-SUMMARY.md` documenting:
- Inventory of 3 MDX files (slug, type, date, duration, word count, screenshot count, ATT&CK techniques referenced).
- Any new entries added to attack-techniques.ts during authoring.
- OPSEC notes per write-up (count of PNGs reviewed; any redactions made; any sample selection rationale notes).
- Bundle metrics post-Plan 06: index-*.js (entry, must be <120 KB gz, NO MDXProvider), ThreeDShell-*.js (3D, <450 KB gz, MDXProvider PRESENT, 3 compiled MDX modules included), WriteupsRoute-*.js (lazy chunk for text shell, ~30 KB gz).
- Deviations from plan (e.g., if a write-up needed an additional section beyond D-15 base, or if a screenshot was dropped in favour of a CodeBlock).
- 3 git commit hashes.
- Phase 3 readiness for Plan 03-07 (deferred content fill + parity audit). All Phase 3 SECTIONS now have content; skills.ts pruning + parity script can reference real tags from these 3 MDX frontmatters.
</output>
